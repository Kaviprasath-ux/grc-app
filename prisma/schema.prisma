// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ==================== CUSTOMER ACCOUNT (MULTI-TENANT) ====================

// CustomerAccount represents a tenant in the multi-tenant system
// All business data is isolated by customerAccountId
model CustomerAccount {
  id                     String                   @id @default(cuid())
  code                   String                   @unique // e.g., GRC_001
  name                   String                   // Customer organization name
  logoUrl                String?                  // Customer logo
  isActive               Boolean                  @default(true)
  isGrcAdded             Boolean                  @default(true) // Whether this account has GRC module access (default true for legacy accounts)
  isTprmAdded            Boolean                  @default(false) // Whether this account was onboarded via TPRM
  emailNotificationsEnabled Boolean               @default(true) // Toggle for non-mandatory email notifications
  createdAt              DateTime                 @default(now())
  updatedAt              DateTime                 @updatedAt
  // Relations - all data belonging to this customer
  users                  User[]
  organizations          Organization[]
  departments            Department[]
  stakeholders           Stakeholder[]
  issues                 Issue[]
  services               Service[]
  frameworks             Framework[]
  requirementCategories  RequirementCategory[]
  requirements           Requirement[]
  requirementExceptions  RequirementException[]
  controls               Control[]
  processes              Process[]
  policies               Policy[]
  evidences              Evidence[]
  artifacts              Artifact[]
  exceptions             Exception[]
  kpis                   KPI[]
  assets                 Asset[]
  risks                  Risk[]
  riskControlMatrixEntries RiskControlMatrixEntry[]
  riskAssessments        RiskAssessment[]
  riskResponses          RiskResponse[]
  audits                 Audit[]
  auditFindings          AuditFinding[]
  capas                  CAPA[]
  internalAuditRisks     InternalAuditRisk[]
  auditableEntities      AuditableEntity[]
  auditEngagements       AuditEngagement[]
  internalAuditFindings  InternalAuditFinding[]
  internalAuditCAPAs     InternalAuditCAPA[]
  auditReports           AuditReport[]
  internalAuditDocuments InternalAuditDocument[]
  governanceVaultDocuments GovernanceVaultDocument[]
  subscriptionPlans      SubscriptionPlan[]
  // Master data relations (tenant-specific)
  regulations            Regulation[]
  controlDomains         ControlDomain[]
  assetCategories        AssetCategory[]
  assetSubCategories     AssetSubCategory[]
  assetGroups            AssetGroup[]
  ciaRatings             CIARating[]
  assetSensitivities     AssetSensitivity[]
  assetCIAClassifications AssetCIAClassification[]
  assetScoringConfigs    AssetScoringConfig[]
  assetScoringRanges     AssetScoringRange[]
  assetLifecycleStatuses AssetLifecycleStatus[]
  assetClassifications   AssetClassification[]
  riskCategories         RiskCategory[]
  riskTypes              RiskType[]
  riskThreats            RiskThreat[]
  riskVulnerabilities    RiskVulnerability[]
  riskCauses             RiskCause[]
  vulnerabilityCategories VulnerabilityCategory[]
  threatCategories       ThreatCategory[]
  controlStrengths       ControlStrength[]
  riskLikelihoods        RiskLikelihood[]
  impactCategories       ImpactCategory[]
  impactRatings          ImpactRating[]
  vulnerabilityRatings   VulnerabilityRating[]
  riskSubCategories      RiskSubCategory[]
  riskRanges             RiskRange[]
  riskScoreConfigs       RiskScoreConfig[]
  auditCategories        AuditCategory[]
  auditNatureOfControls  AuditNatureOfControl[]
  auditRiskFactors       AuditRiskFactor[]
  auditProbabilities     AuditProbability[]
  auditImpacts           AuditImpact[]
  auditScoringRanges     AuditScoringRange[]
  auditScoringConfigs    AuditScoringConfig[]
  auditPeriodicities     AuditPeriodicity[]
  auditEscalationConfigs AuditEscalationConfig[]
  auditTypes             AuditType[]
  auditLocations         AuditLocation[]
  processFrequencies     ProcessFrequency[]
  natureOfImplementations NatureOfImplementation[]
  organizationLocations  OrganizationLocation[]
  designations           Designation[]
  userDocumentTypes      UserDocumentType[]
  internalAuditProcesses InternalAuditProcess[]
  governanceTemplates    GovernanceTemplate[]
  // Notification relations
  notifications              Notification[]
  notificationPreferences    NotificationPreference[]
  // BIA Settings relations (tenant-specific)
  biaCategories              BIACategory[]
  biaRatings                 BIARating[]
  biaScoringConfigs          BIAScoringConfig[]
  biaScoringRanges           BIAScoringRange[]
  bcpLabels                  BCPLabel[]
  dynamicTranslations        DynamicTranslation[]
  regulatoryProfiles         RegulatoryProfile[]
  suggestedRegulations       SuggestedRegulation[]
  // TPRM relations
  tprmVendors                TPRMVendor[]
  tprmAssessments            TPRMAssessment[]
  tprmAssessmentLogs         TPRMAssessmentLog[]
  tprmConfiguration          TPRMConfiguration?
  tprmVendorProfileFields    TPRMVendorProfileField[]
  tprmOnboardingQuestions    TPRMOnboardingQuestion[]
  tprmServiceCategories      TPRMServiceCategory[]
  tprmDisciplines            TPRMDiscipline[]
  tprmDepartments            TPRMDepartment[]
  tprmQuestionnaireTemplates TPRMQuestionnaireTemplate[]
  tprmOffboardingQuestions   TPRMOffboardingQuestion[]
  tprmScorecardFactors       TPRMScorecardFactor[]
  tprmDomains                TPRMDomain[]
  tprmMasterQuestions        TPRMMasterQuestion[]
  tprmQuestionnaireQuestions TPRMQuestionnaireQuestion[]
  tprmMonitoringVendors      TPRMMonitoringVendor[]
  tprmMonitoringAssessments  TPRMMonitoringAssessment[]
  tprmMonitoringSchedule     TPRMMonitoringSchedule?
  tprmAssessmentResponses    TPRMAssessmentResponse[]
  tprmClarifications         TPRMClarification[]
  tprmIssueRemediations      TPRMIssueRemediation[]
  tprmVendorIssues           TPRMVendorIssue[]
}

// Subscription Plan for Customer Accounts
model SubscriptionPlan {
  id                  String          @id @default(cuid())
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  startDate           DateTime
  expiryDate          DateTime
  maxFrameworksAllowed Int            @default(0)
  maxAccountsAllowed   Int            @default(0)
  assessmentLimit     Int             @default(0) // TPRM: max assessments allowed
  vendorLimit         Int             @default(0) // TPRM: max vendors allowed
  frameworksUsed      Int             @default(0)
  accountsUsed        Int             @default(0)
  status              String          @default("Active") // Active, Inactive, Expired
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
}

// ==================== ROLE-BASED ACCESS CONTROL (RBAC) ====================

// Roles (GRCAdministrator, CustomerAdministrator, AuditHead, etc.)
model Role {
  id          String           @id @default(cuid())
  name        String           @unique
  description String?
  isSystem    Boolean          @default(false) // System roles cannot be deleted
  permissions RolePermission[]
  users       UserRole[]
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
}

// Permissions (resource + action + scope)
model Permission {
  id        String           @id @default(cuid())
  resource  String           // e.g., "organization.process", "risk.register", "audit.dashboard"
  action    String           // e.g., "view", "create", "edit", "delete", "approve"
  scope     String           @default("all") // "all", "department", "own"
  roles     RolePermission[]
  createdAt DateTime         @default(now())

  @@unique([resource, action, scope])
}

// Role-Permission Junction Table
model RolePermission {
  id           String     @id @default(cuid())
  roleId       String
  permissionId String
  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)
  createdAt    DateTime   @default(now())

  @@unique([roleId, permissionId])
}

// User-Role Junction Table (users can have multiple roles)
model UserRole {
  id        String   @id @default(cuid())
  userId    String
  roleId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  role      Role     @relation(fields: [roleId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, roleId])
}

// ==================== ORGANIZATION MODULE ====================

// Organization Profile
model Organization {
  id                 String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId  String
  customerAccount    CustomerAccount  @relation(fields: [customerAccountId], references: [id])
  name               String
  email              String?
  phone              String?
  logo               String?
  establishedDate    String?
  employeeCount      Int              @default(0)
  branchCount        Int              @default(0)
  headOfficeLocation String?
  headOfficeAddress  String?
  website            String?
  description        String?
  vision             String?
  mission            String?
  value              String?
  ceoMessage         String?
  facebook           String?
  youtube            String?
  twitter            String?
  linkedin           String?
  brochure           String?
  createdAt          DateTime         @default(now())
  updatedAt          DateTime         @updatedAt
  branches           Branch[]
  dataCenters        DataCenter[]
  cloudProviders     CloudProvider[]

  @@index([customerAccountId])
}

// Branch Office
model Branch {
  id             String       @id @default(cuid())
  location       String
  address        String
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
}

// Data Center
model DataCenter {
  id             String       @id @default(cuid())
  locationType   String       // On-Prem or Outsourced
  address        String?      // For On-Prem
  vendor         String?      // For Outsourced
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
}

// Cloud Provider
model CloudProvider {
  id             String       @id @default(cuid())
  name           String
  serviceType    String       // Iaas, Paas, Saas
  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
}

// Departments
model Department {
  id                    String                 @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String
  customerAccount       CustomerAccount        @relation(fields: [customerAccountId], references: [id])
  name                  String
  description           String?
  headId                String?
  createdAt             DateTime               @default(now())
  updatedAt             DateTime               @updatedAt
  users                 User[]
  issues                Issue[]
  stakeholders          Stakeholder[]
  controls              Control[]
  processes             Process[]
  policies              Policy[]
  assets                Asset[]
  risks                 Risk[]
  audits                Audit[]
  evidences             Evidence[]
  exceptions            Exception[]
  requirementExceptions RequirementException[]
  kpis                  KPI[]
  internalAuditRisks    InternalAuditRisk[]    @relation("InternalAuditDepartment")
  auditableEntities     AuditableEntity[]
  auditEngagements      AuditEngagement[]
  internalAuditFindings InternalAuditFinding[]
  tprmVendors           TPRMVendor[]           @relation("TPRMVendorDepartment")

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Users
model User {
  id                  String           @id @default(cuid())
  userId              String           @unique
  userName            String
  email               String
  password            String?
  firstName           String
  lastName            String
  fullName            String
  designation         String?
  function            String?
  role                String           @default("User")
  language            String           @default("English")
  timezone            String           @default("UTC")
  isActive            Boolean          @default(true)
  isBlocked           Boolean          @default(false)
  // Multi-tenant: Link user to customer account
  customerAccountId   String?
  customerAccount     CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // CustomerAdmin fields (legacy - kept for backwards compatibility)
  customerCode        String?          // Customer account code (e.g., GRC_001)
  logoUrl             String?          // Customer logo URL
  lastLogin           DateTime?        // Last login timestamp
  departmentId        String?
  department          Department?      @relation(fields: [departmentId], references: [id])
  // Audit Head assignment - links auditee to their audit head
  auditHeadId         String?
  auditHead           User?            @relation("AuditHeadAuditees", fields: [auditHeadId], references: [id])
  managedAuditees     User[]           @relation("AuditHeadAuditees")
  // Reporting Manager - for organization hierarchy
  reportingManagerId  String?
  reportingManager    User?            @relation("ReportingManagerReports", fields: [reportingManagerId], references: [id])
  directReports       User[]           @relation("ReportingManagerReports")
  // Creator - tracks who created this user (for DR/DC framework visibility)
  createdById         String?
  createdBy           User?            @relation("UserCreator", fields: [createdById], references: [id])
  createdUsers        User[]           @relation("UserCreator")
  createdAt           DateTime    @default(now())
  updatedAt           DateTime    @updatedAt
  // Relations
  ownedControls       Control[]   @relation("ControlOwner")
  assignedControls    Control[]   @relation("ControlAssignee")
  ownedRisks          Risk[]      @relation("RiskOwner")
  ownedProcesses      Process[]   @relation("ProcessOwner")
  ownedAssets         Asset[]     @relation("AssetOwner")
  custodianAssets     Asset[]     @relation("AssetCustodian")
  assignedEvidences   Evidence[]  @relation("EvidenceAssignee")
  auditor             Audit[]     @relation("Auditor")
  assignedPolicies    Policy[]    @relation("PolicyAssignee")
  approvedPolicies    Policy[]    @relation("PolicyApprover")
  requestedExceptions Exception[] @relation("ExceptionRequester")
  approverExceptions  Exception[] @relation("ExceptionApprover")
  uploadedArtifacts   Artifact[]  @relation("ArtifactUploader")
  // RBAC relations
  userRoles           UserRole[]
  // Internal Audit relations
  assignedEngagements AuditEngagement[]   @relation("EngagementAuditor")
  auditeeEngagements  AuditEngagement[]   @relation("EngagementAuditee")
  memberOfEngagements AuditEngagement[]   @relation("EngagementTeamMembers")
  // Audit Head owned data
  ownedEngagements    AuditEngagement[]   @relation("AuditHeadEngagements")
  engagementComments  EngagementComment[] @relation("EngagementComments")
  ownedAuditRisks     InternalAuditRisk[] @relation("AuditHeadRisks")
  ownedAuditFindings  InternalAuditFinding[] @relation("AuditHeadFindings")
  ownedAuditCAPAs     InternalAuditCAPA[] @relation("AuditHeadCAPAs")
  ownedAuditReports   AuditReport[]       @relation("AuditHeadReports")
  ownedAuditDocuments InternalAuditDocument[] @relation("AuditHeadDocuments")
  // Audit Head owned settings (each Audit Head has their own settings)
  ownedCategories         AuditCategory[]          @relation("AuditHeadCategories")
  ownedNatureOfControls   AuditNatureOfControl[]   @relation("AuditHeadNatureOfControls")
  ownedRiskFactors        AuditRiskFactor[]        @relation("AuditHeadRiskFactors")
  ownedProbabilities      AuditProbability[]       @relation("AuditHeadProbabilities")
  ownedImpacts            AuditImpact[]            @relation("AuditHeadImpacts")
  ownedScoringRanges      AuditScoringRange[]      @relation("AuditHeadScoringRanges")
  ownedScoringConfigs     AuditScoringConfig[]     @relation("AuditHeadScoringConfigs")
  ownedPeriodicities      AuditPeriodicity[]       @relation("AuditHeadPeriodicities")
  ownedEscalationConfigs  AuditEscalationConfig[]  @relation("AuditHeadEscalationConfigs")
  ownedAuditTypes         AuditType[]              @relation("AuditHeadTypes")
  ownedLocations          AuditLocation[]          @relation("AuditHeadLocations")
  ownedProcessFrequencies ProcessFrequency[]       @relation("AuditHeadProcessFrequencies")
  ownedNatureOfImplementations NatureOfImplementation[] @relation("AuditHeadNatureOfImplementations")
  ownedInternalAuditProcesses InternalAuditProcess[] @relation("AuditHeadProcesses")
  // Process RACI relations
  responsibleProcesses Process[]          @relation("ProcessResponsible")
  accountableProcesses Process[]          @relation("ProcessAccountable")
  consultedProcesses   Process[]          @relation("ProcessConsulted")
  informedProcesses    Process[]          @relation("ProcessInformed")
  // Issue relations
  ownedIssues          Issue[]       @relation("IssueOwner")
  createdActions       IssueAction[]
  // AI relations
  aiOperations        AIOperation[]
  aiJobs              AIJob[]
  // Governance Templates
  uploadedTemplates   GovernanceTemplate[] @relation("TemplateUploader")
  // Notification relations
  notifications              Notification[]
  notificationPreferences    NotificationPreference[]
  // OAuth/SSO relations
  oauthAccounts              OAuthAccount[]
  // TPRM fields
  tprmRole                   String?           // Approver, Assessor, Auditor, BusinessOwner, InternalITTeam, RelationshipManager
  tprmFunctionCategory       String?           // "TPRM Team" or "Business"
  // TPRM relations
  tprmInitiated              TPRMAssessment[]  @relation("TPRMInitiator")
  tprmAssessed               TPRMAssessment[]  @relation("TPRMAssessor")
  tprmApproved               TPRMAssessment[]  @relation("TPRMApprover")
  tprmResponsesDelegated     TPRMAssessmentResponse[] @relation("TPRMResponseDelegatedTo")
  tprmResponsesResponded     TPRMAssessmentResponse[] @relation("TPRMResponseRespondedBy")
  tprmClarificationsRequested TPRMClarification[]     @relation("TPRMClarificationRequestedBy")
  tprmVendorIssuesReported   TPRMVendorIssue[]        @relation("TPRMVendorIssueReportedBy")

  // Multi-tenant: userName and email are unique per customer account, not globally
  @@unique([customerAccountId, userName])
  @@unique([customerAccountId, email])
  @@index([customerAccountId])
}

// OAuth/SSO Accounts (audit trail for SSO logins)
model OAuthAccount {
  id                String   @id @default(cuid())
  userId            String
  user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  provider          String   // "google" or "microsoft-entra-id"
  providerAccountId String
  email             String
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([provider, providerAccountId])
  @@index([userId])
}

// Stakeholders
model Stakeholder {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount    @relation(fields: [customerAccountId], references: [id])
  name              String
  email             String?
  type              String             @default("Internal") // Internal, External, Third Party
  status            String             @default("Active") // Active, Inactive
  departmentId      String?
  department        Department?        @relation(fields: [departmentId], references: [id])
  issues            IssueStakeholder[]
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  @@index([customerAccountId])
}

// Issues
model Issue {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount    @relation(fields: [customerAccountId], references: [id])
  title             String
  description       String?
  domain            String // Internal, External, IT, GRC
  category          String // Finance, Human Resources, Data breach, etc.
  issueType         String? // Financial, Operational, Compliance, Security, etc.
  status            String             @default("Open") // Open, In Progress, Pending, Resolved, Closed
  dueDate           DateTime?
  departmentId      String?
  department        Department?        @relation(fields: [departmentId], references: [id])
  ownerId           String?
  owner             User?              @relation("IssueOwner", fields: [ownerId], references: [id])
  actions           IssueAction[]
  regulations       IssueRegulation[]
  processes         IssueProcess[]
  stakeholders      IssueStakeholder[]
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  @@index([customerAccountId])
}

// Issue Actions (created by DepartmentReviewer, reviewed by CustomerAdmin/Reviewer)
model IssueAction {
  id            String               @id @default(cuid())
  issueId       String
  issue         Issue                @relation(fields: [issueId], references: [id], onDelete: Cascade)
  actionType    String // "Preventive", "Corrective"
  description   String
  completion    Int                  @default(0) // Completion percentage (0-100)
  comment       String?
  status        String               @default("Pending") // "Pending", "Resolved", "Sent Back"
  fileName      String?              // Original file name
  fileType      String?              // File extension/type
  filePath      String?              // Path to stored file
  fileSize      Int?                 // File size in bytes
  createdById   String
  createdBy     User                 @relation(fields: [createdById], references: [id])
  comments      IssueActionComment[]
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt
}

// Comments on Issue Actions (for resend flow)
model IssueActionComment {
  id        String      @id @default(cuid())
  actionId  String
  action    IssueAction @relation(fields: [actionId], references: [id], onDelete: Cascade)
  comment   String
  createdBy String // User name or organization name
  createdAt DateTime    @default(now())
}

// Issue-Regulation Junction Table
model IssueRegulation {
  id           String     @id @default(cuid())
  issueId      String
  issue        Issue      @relation(fields: [issueId], references: [id], onDelete: Cascade)
  regulationId String
  regulation   Regulation @relation(fields: [regulationId], references: [id], onDelete: Cascade)
  createdAt    DateTime   @default(now())

  @@unique([issueId, regulationId])
}

// Issue-Process Junction Table
model IssueProcess {
  id        String   @id @default(cuid())
  issueId   String
  issue     Issue    @relation(fields: [issueId], references: [id], onDelete: Cascade)
  processId String
  process   Process  @relation(fields: [processId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([issueId, processId])
}

// Issue-Stakeholder Junction Table
model IssueStakeholder {
  id            String      @id @default(cuid())
  issueId       String
  issue         Issue       @relation(fields: [issueId], references: [id], onDelete: Cascade)
  stakeholderId String
  stakeholder   Stakeholder @relation(fields: [stakeholderId], references: [id], onDelete: Cascade)
  needExpectation String?   // What the stakeholder needs/expects from this issue resolution
  createdAt     DateTime    @default(now())

  @@unique([issueId, stakeholderId])
}

// Services
model Service {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  title             String
  description       String?
  serviceUser       String // Internal, External, Public
  serviceCategory   String?
  serviceItem       String?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
}

// ==================== COMPLIANCE MODULE ====================

// Frameworks
model Framework {
  id                    String                @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String
  customerAccount       CustomerAccount       @relation(fields: [customerAccountId], references: [id])
  code                  String?
  name                  String
  description           String?
  version               String?
  type                  String                @default("Framework") // Framework, Standard, Regulation
  status                String                @default("Subscribed") // Subscribed, Not Subscribed, Suggested, Archived
  country               String?
  industry              String?
  isCustom              Boolean               @default(false)
  // Master template flag: true = this is a master template that customers can subscribe to
  isMasterTemplate      Boolean               @default(false)
  // Source framework: links a customer's subscribed copy back to the original master
  sourceFrameworkId     String?
  sourceFramework       Framework?            @relation("FrameworkSubscriptions", fields: [sourceFrameworkId], references: [id], onDelete: SetNull)
  subscribedCopies      Framework[]           @relation("FrameworkSubscriptions")
  logo                  String?
  supportDocumentUrl    String?
  compliancePercentage  Float                 @default(0)
  policyPercentage      Float                 @default(0)
  evidencePercentage    Float                 @default(0)
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  controls              Control[]
  evidences             Evidence[]
  requirements          Requirement[]
  requirementCategories RequirementCategory[]
  exceptions            Exception[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
  @@index([sourceFrameworkId])
}

// Requirement Categories (Domains/Chapters in a framework)
model RequirementCategory {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  name              String
  code              String?
  description       String?
  sortOrder         Int             @default(0)
  frameworkId       String
  framework         Framework       @relation(fields: [frameworkId], references: [id], onDelete: Cascade)
  requirements      Requirement[]
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([frameworkId, name])
  @@index([customerAccountId])
}

// Requirements (Framework Requirements with hierarchy)
model Requirement {
  id                   String                 @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId    String
  customerAccount      CustomerAccount        @relation(fields: [customerAccountId], references: [id])
  code                 String
  name                 String
  description          String?
  requirementType      String                 @default("Mandatory") // Mandatory, Additional
  chapterType          String                 @default("Domain") // Domain, Process Domain, Technical Domain
  sortOrder            Int                    @default(0)
  level                Int                    @default(1) // Hierarchy level: 1=category, 2=requirement, 3=sub-requirement
  parentId             String?
  parent               Requirement?           @relation("RequirementHierarchy", fields: [parentId], references: [id])
  children             Requirement[]          @relation("RequirementHierarchy")
  frameworkId          String
  framework            Framework              @relation(fields: [frameworkId], references: [id], onDelete: Cascade)
  categoryId           String?
  category             RequirementCategory?   @relation(fields: [categoryId], references: [id])
  // SOA fields
  applicability        String? // Yes, No
  justification        String?
  implementationStatus String? // Yes, No, Ongoing, N/A
  controlCompliance    String? // Compliant, Non Compliant, Partial Compliant
  // Relations
  controls             RequirementControl[]
  exceptions           RequirementException[]
  complianceExceptions Exception[]
  createdAt            DateTime               @default(now())
  updatedAt            DateTime               @updatedAt

  @@unique([frameworkId, code])
  @@index([customerAccountId])
}

// Requirement-Control Many-to-Many Junction Table
model RequirementControl {
  id            String      @id @default(cuid())
  requirementId String
  requirement   Requirement @relation(fields: [requirementId], references: [id], onDelete: Cascade)
  controlId     String
  control       Control     @relation(fields: [controlId], references: [id], onDelete: Cascade)
  createdAt     DateTime    @default(now())

  @@unique([requirementId, controlId])
}

// Requirement Exceptions (for compliance exceptions from requirements)
model RequirementException {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  code              String
  name              String
  description       String?
  category          String          @default("Compliance") // Policy, Control, Compliance, Risk
  requirementId     String
  requirement       Requirement     @relation(fields: [requirementId], references: [id], onDelete: Cascade)
  departmentId      String?
  department        Department?     @relation(fields: [departmentId], references: [id])
  requesterId       String?
  status            String          @default("Pending") // Pending, Approved, Authorised, Submitted for Closure, Overdue, RiskAccepted, Closed
  endDate           DateTime?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, code])
  @@index([customerAccountId])
}

// Regulations (legacy - keeping for backwards compatibility)
model Regulation {
  id                     String            @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId      String?
  customerAccount        CustomerAccount?  @relation(fields: [customerAccountId], references: [id])
  name                   String
  version                String?
  sa1Date                String?
  sa2Date                String?
  scope                  String?
  exclusionJustification String?
  document               String?
  certificate            String?
  status                 String            @default("Subscribed") // Subscribed, Not Subscribed
  issues                 IssueRegulation[]
  attachments            RegulationAttachment[]
  createdAt              DateTime          @default(now())
  updatedAt              DateTime          @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

model RegulationAttachment {
  id           String     @id @default(cuid())
  regulationId String
  regulation   Regulation @relation(fields: [regulationId], references: [id], onDelete: Cascade)
  type         String     // "document" or "certificate"
  fileName     String
  fileType     String?
  fileSize     Int?
  filePath     String
  uploadedAt   DateTime   @default(now())
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
}

// Control Domains
model ControlDomain {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  code              String?
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  controls          Control[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Controls
model Control {
  id                       String               @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId        String
  customerAccount          CustomerAccount      @relation(fields: [customerAccountId], references: [id])
  controlCode              String
  name                     String
  description              String?
  controlQuestion          String?
  functionalGrouping       String? // Govern, Identify, Protect, Detect, Respond, Recover
  status                   String               @default("Non Compliant") // Non Compliant, Compliant, Not Applicable, Partial Compliant
  entities                 String               @default("Organization Wide") // Organization Wide
  isControlList            Boolean              @default(false)
  relativeControlWeighting Int?
  scope                    String? // In-Scope, Not In-Scope
  // CMM Maturity Level Descriptions
  notPerformed             String? // CMM Level 0
  performedInformally      String? // CMM Level 1
  plannedAndTracked        String? // CMM Level 2
  wellDefined              String? // CMM Level 3
  quantitativelyControlled String? // CMM Level 4
  continuouslyImproving    String? // CMM Level 5
  // Relations
  domainId                 String?
  domain                   ControlDomain?       @relation(fields: [domainId], references: [id])
  frameworkId              String?
  framework                Framework?           @relation(fields: [frameworkId], references: [id])
  departmentId             String?
  department               Department?          @relation(fields: [departmentId], references: [id])
  ownerId                  String?
  owner                    User?                @relation("ControlOwner", fields: [ownerId], references: [id])
  assigneeId               String?
  assignee                 User?                @relation("ControlAssignee", fields: [assigneeId], references: [id])
  createdAt                DateTime             @default(now())
  updatedAt                DateTime             @updatedAt
  evidences                Evidence[]
  exceptions               Exception[]
  requirements             RequirementControl[]
  controlRisks             ControlRisk[]
  policyControls           PolicyControl[]
  evidenceControls         EvidenceControl[]      @relation("EvidenceControls")
  // Risk Control Matrix control links
  riskControlMatrixControls RiskControlMatrixControl[]

  @@unique([customerAccountId, controlCode])
  @@index([customerAccountId])
}

// Processes
model Process {
  id                     String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId      String
  customerAccount        CustomerAccount @relation(fields: [customerAccountId], references: [id])
  processCode            String
  name                   String
  description            String?
  processType            String          @default("Primary") // Primary, Management, Supporting
  departmentId           String?
  department             Department?     @relation(fields: [departmentId], references: [id])
  ownerId                String?
  owner                  User?           @relation("ProcessOwner", fields: [ownerId], references: [id])
  status                 String          @default("Active")
  // Audit Library fields
  processFrequency       String?         // Annually, As needed, Bi-annually, Daily, monthly, Monthly, Quarterly, Weekly
  natureOfImplementation String?         // Manual, Automated, Manual + Automated
  riskRating             String?         // Low, Medium, High, Not Assessed
  assetDependency        Boolean         @default(false)
  assetId                String?         // Link to Asset when assetDependency is true
  asset                  Asset?          @relation("ProcessAsset", fields: [assetId], references: [id])
  externalDependency     Boolean         @default(false)
  externalParty          String?         // Name of external party when externalDependency is true
  location               String?         // JSON array of locations
  kpiMeasurementRequired Boolean         @default(false)
  // KPI Configuration fields
  kpiObjective           String?
  kpiDataSource          String?
  kpiExpectedValue       Float?
  kpiDescription         String?
  kpiFormula             String?
  kpiTargetedValue       Float?
  piiCapture             Boolean         @default(false)
  recurrence             String?         // Daily, Weekly, Monthly, Quarterly, Annually
  reviewDate             DateTime?       // Review date for KPI or PII
  operationalComplexity  String?         // Low, Medium, High
  lastAuditDate          DateTime?
  // RACI fields
  responsibleId          String?
  responsible            User?           @relation("ProcessResponsible", fields: [responsibleId], references: [id])
  accountableId          String?
  accountable            User?           @relation("ProcessAccountable", fields: [accountableId], references: [id])
  consultedId            String?
  consulted              User?           @relation("ProcessConsulted", fields: [consultedId], references: [id])
  informedId             String?
  informed               User?           @relation("ProcessInformed", fields: [informedId], references: [id])
  createdAt              DateTime        @default(now())
  updatedAt              DateTime        @updatedAt
  // BIA Relation
  biaAssessment          ProcessBIA?
  // Issue Relation
  issues                 IssueProcess[]
  // Risks impacted by this process
  impactedByRisks        Risk[]          @relation("RiskImpactedProcess")
  // Attachments
  attachments            ProcessAttachment[]
  // Audit Engagements
  auditEngagements       AuditEngagement[]   @relation("EngagementProcess")

  @@unique([customerAccountId, processCode])
  @@index([customerAccountId])
}

// Process Attachment (files associated with processes)
model ProcessAttachment {
  id         String   @id @default(cuid())
  fileName   String
  fileType   String?  // pdf, docx, xlsx, png, jpg, etc.
  fileSize   Int?
  filePath   String
  category   String   @default("process_document") // "process_document" or "employee_onboarding"
  uploadedAt DateTime @default(now())
  processId  String
  process    Process  @relation(fields: [processId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

// ==================== BUSINESS IMPACT ANALYSIS (BIA) ====================

// BIA Impact Categories (Financial, Reputational, Regulatory, Safety, Operational)
model BIACategory {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  description       String?
  sortOrder         Int             @default(0)
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// BIA Rating Methodology (defines the rating levels and their scores)
model BIARating {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  label             String          // High, Medium, Low
  score             Int             @default(0) // Score value for calculations
  description       String?         // Description of what this rating means
  color             String?         // Color code for UI display
  sortOrder         Int             @default(0)
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, label])
  @@index([customerAccountId])
}

// BIA Scoring Configuration (High of All, Addition of All, Product of All)
model BIAScoringConfig {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account (optional for backward compatibility)
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  calculationType   String           @default("High of all") // High of all, Addition of all, Product of all
  isActive          Boolean          @default(true)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([customerAccountId])
}

// BIA Scoring Ranges (defines criticality levels based on scores)
model BIAScoringRange {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  label             String          // Critical, High, Medium, Low
  lowValue          Int             @default(0)
  highValue         Int?
  color             String?         // Color code for UI display
  calculationType   String          @default("High of all") // Links to the scoring config
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, label, calculationType])
  @@index([customerAccountId])
}

// BCP Labels (Business Continuity Planning labels for RTO/RPO)
model BCPLabel {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  type              String          // RTO, RPO
  hours             Int             @default(0) // Value in hours
  description       String?
  sortOrder         Int             @default(0)
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Process BIA Assessment (stores the BIA assessment for each process)
model ProcessBIA {
  id                 String              @id @default(cuid())
  processId          String              @unique
  process            Process             @relation(fields: [processId], references: [id], onDelete: Cascade)
  // BIA Status
  status             String              @default("Open") // Open, Pending Approval, Approved, Rejected
  // Calculated values
  impactRating       Int?                // Calculated based on scoring config
  processCriticality String?             // Critical, High, Medium, Low (derived from scoring ranges)
  // Recovery objectives
  rtoHours           Int                 @default(0) // Recovery Time Objective in hours
  rpoHours           Int                 @default(0) // Recovery Point Objective in hours
  rtoLabel           String?             // Label for RTO
  rpoLabel           String?             // Label for RPO
  // Scoring range values
  lowValue           Int?
  criticalValue      Int?
  highValue          Int?
  mediumValue        Int?
  // Approver
  approverId         String?
  approverName       String?
  approvedAt         DateTime?
  // Comments
  comments           String?
  rejectionReason    String?
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  // Category ratings
  categoryRatings    ProcessBIARating[]
  // BIA Comments (for send back flow)
  biaComments        ProcessBIAComment[]
}

// Process BIA Comments (for tracking send back comments)
model ProcessBIAComment {
  id           String     @id @default(cuid())
  processBIAId String
  processBIA   ProcessBIA @relation(fields: [processBIAId], references: [id], onDelete: Cascade)
  comment      String
  createdBy    String     // User ID
  createdByName String    // User name at time of comment
  action       String     // "SendBack", "Approve", "Submit"
  createdAt    DateTime   @default(now())
}

// Process BIA Category Ratings (individual category ratings for a process)
model ProcessBIARating {
  id            String     @id @default(cuid())
  processBIAId  String
  processBIA    ProcessBIA @relation(fields: [processBIAId], references: [id], onDelete: Cascade)
  categoryName  String     // Financial, Reputational, Regulatory, Safety, Operational
  rating        String?    // High, Medium, Low
  ratingScore   Int?       // Score value
  description   String?    // Impact description
  createdAt     DateTime   @default(now())
  updatedAt     DateTime   @updatedAt

  @@unique([processBIAId, categoryName])
}

// Governance - Policies, Standards, Procedures
model Policy {
  id                    String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String
  customerAccount       CustomerAccount    @relation(fields: [customerAccountId], references: [id])
  code                  String
  name                  String
  version               String             @default("1.0")
  documentType          String             @default("Policy") // Policy, Standard, Procedure
  recurrence            String? // Weekly, Monthly, Quarterly, Yearly
  departmentId          String?
  department            Department?        @relation(fields: [departmentId], references: [id])
  assigneeId            String?
  assignee              User?              @relation("PolicyAssignee", fields: [assigneeId], references: [id])
  approverId            String?
  approver              User?              @relation("PolicyApprover", fields: [approverId], references: [id])
  status                String             @default("Not Uploaded") // Not Uploaded, Draft, Approved, Needs Review, Published, Pending Approval
  effectiveDate         DateTime?
  reviewDate            DateTime?
  content               String?
  // AI Ingest Fields
  aiIngestStatus        String?            // NOT_STARTED, QUEUED, PROCESSING, INGESTED, FAILED
  aiIngestedAt          DateTime?
  aiIngestJobId         String?            // RunPod job ID for polling
  // AI Review Fields
  aiReviewStatus        String?            @default("Pending") // Pending, In Progress, Completed
  aiReviewScore         Float?             @default(0)
  aiReviewJustification String?
  createdAt             DateTime           @default(now())
  updatedAt             DateTime           @updatedAt
  attachments           PolicyAttachment[]
  policyControls        PolicyControl[]
  policyAIReviews       PolicyAIReview[]
  policyExceptions      PolicyException[]
  exceptions            Exception[]        @relation("PolicyExceptions")
  vaultDocumentLinks    GovernanceVaultDocumentLink[]

  @@unique([customerAccountId, code])
  @@index([customerAccountId])
}

// Evidence
model Evidence {
  id                    String               @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String
  customerAccount       CustomerAccount      @relation(fields: [customerAccountId], references: [id])
  evidenceCode          String
  name                  String
  description           String?
  domain                String? // Compliance, Change Management, Education, Risk Management, etc.
  frameworkId           String?
  framework             Framework?           @relation(fields: [frameworkId], references: [id])
  controlId             String? // Legacy single control reference
  control               Control?             @relation(fields: [controlId], references: [id])
  departmentId          String?
  department            Department?          @relation(fields: [departmentId], references: [id])
  assigneeId            String?
  assignee              User?                @relation("EvidenceAssignee", fields: [assigneeId], references: [id])
  dueDate               DateTime?
  reviewDate            DateTime?
  recurrence            String? // Yearly, Half-yearly, Quarterly, Monthly
  status                String               @default("Not Uploaded") // Not Uploaded, Draft, Validated, Published, Need Attention
  publishedAt           DateTime?
  // KPI fields
  kpiRequired           Boolean              @default(false)
  kpiObjective          String?
  kpiDataSource         String?
  kpiExpectedScore      Float?
  kpiDescription        String?
  kpiCalculationFormula String?
  // AI status fields
  aiIngestStatus        String?              // NOT_STARTED, QUEUED, PROCESSING, INGESTED, FAILED
  aiIngestedAt          DateTime?
  aiReviewStatus        String?              // NOT_READY, IN_PROGRESS, COMPLETED, FAILED
  aiReviewedAt          DateTime?
  createdAt             DateTime             @default(now())
  updatedAt             DateTime             @updatedAt
  attachments           EvidenceAttachment[]
  kpis                  KPI[]
  evidenceControls      EvidenceControl[]
  linkedArtifacts       EvidenceArtifact[]
  evidenceAIReviews     EvidenceAIReview[]
  evidenceAIIngestJobs  EvidenceAIIngestJob[]
  evidenceAIIngestResults EvidenceAIIngestResult[]
  cycleComments          EvidenceCycleComment[]

  @@unique([customerAccountId, evidenceCode])
  @@index([customerAccountId])
}

// Evidence Cycle Comments for Send Back / Resubmit workflow
model EvidenceCycleComment {
  id          String   @id @default(cuid())
  evidenceId  String
  evidence    Evidence @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  cyclePeriod String   // e.g. "Jan-Dec", "Q1", "Jan"
  action      String   // "sendback" or "resubmit"
  comment     String
  userId      String?
  userName    String?
  createdAt   DateTime @default(now())

  @@index([evidenceId, cyclePeriod])
}

// Evidence-Control Many-to-Many Junction Table
model EvidenceControl {
  id         String   @id @default(cuid())
  evidenceId String
  evidence   Evidence @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  controlId  String
  control    Control  @relation("EvidenceControls", fields: [controlId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())

  @@unique([evidenceId, controlId])
}

// Standalone Artifact for the Artifacts tab
model Artifact {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount    @relation(fields: [customerAccountId], references: [id])
  artifactCode      String
  name              String
  fileName          String
  fileType          String? // pdf, docx, xlsx, png, jpg, etc.
  fileSize          Int?
  filePath          String
  uploadedBy        String?
  uploadedById      String?
  uploader          User?              @relation("ArtifactUploader", fields: [uploadedById], references: [id])
  aiReviewStatus    String? // Pending, Reviewed
  aiReviewScore     Float?
  aiReviewNotes     String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  linkedEvidences   EvidenceArtifact[]

  // Relation to AI reviews where this artifact was used
  evidenceAIReviews EvidenceAIReview[]

  @@unique([customerAccountId, artifactCode])
  @@index([customerAccountId])
}

// Evidence-Artifact Many-to-Many Junction Table
model EvidenceArtifact {
  id         String   @id @default(cuid())
  evidenceId String
  evidence   Evidence @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  artifactId String
  artifact   Artifact @relation(fields: [artifactId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())

  @@unique([evidenceId, artifactId])
}

// Exceptions
model Exception {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount    @relation(fields: [customerAccountId], references: [id])
  exceptionCode     String
  name              String
  description       String? // Reason For Exception
  category          String // Policy, Control, Compliance, Risk
  departmentId      String?
  department        Department?        @relation(fields: [departmentId], references: [id])
  // Category-specific references
  controlId         String?
  control           Control?           @relation(fields: [controlId], references: [id])
  policyId          String?
  policy            Policy?            @relation("PolicyExceptions", fields: [policyId], references: [id])
  riskId            String?
  risk              Risk?              @relation("RiskExceptions", fields: [riskId], references: [id])
  // Compliance category references
  frameworkId       String?
  framework         Framework?         @relation(fields: [frameworkId], references: [id])
  requirementId     String?
  requirement       Requirement?       @relation(fields: [requirementId], references: [id])
  // Requester and Approver
  requesterId       String?
  requester         User?              @relation("ExceptionRequester", fields: [requesterId], references: [id])
  approverId        String?
  approver          User?              @relation("ExceptionApprover", fields: [approverId], references: [id])
  // Approval details
  approvedBy        String? // Name of person who approved
  approvedDate      DateTime?
  // Status and dates
  status            String             @default("Pending") // Pending, Approved, Authorised, Submitted for Closure, Overdue, RiskAccepted, Closed
  startDate         DateTime?
  endDate           DateTime?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  // Relations
  comments          ExceptionComment[]
  policyExceptions  PolicyException[]

  @@unique([customerAccountId, exceptionCode])
  @@index([customerAccountId])
}

// Exception Comments
model ExceptionComment {
  id          String    @id @default(cuid())
  content     String
  exceptionId String
  exception   Exception @relation(fields: [exceptionId], references: [id], onDelete: Cascade)
  userId      String?
  userName    String? // Store username at time of comment
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

// KPI (Key Performance Indicators)
model KPI {
  id                 String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId  String
  customerAccount    CustomerAccount @relation(fields: [customerAccountId], references: [id])
  code               String
  objective          String? // KPI Objective
  description        String? // KPI Description
  dataSource         String? // KPI Data Source
  calculationFormula String? // KPI Calculation Formula
  expectedScore      Float? // Expected Score
  actualScore        Float? // Current Actual Score
  reviewDate         DateTime? // Next Review Date
  status             String          @default("Scheduled") // Scheduled, Missed, Overdue, Achieved
  departmentId       String?
  department         Department?     @relation(fields: [departmentId], references: [id])
  evidenceId         String?
  evidence           Evidence?       @relation(fields: [evidenceId], references: [id], onDelete: SetNull)
  createdAt          DateTime        @default(now())
  updatedAt          DateTime        @updatedAt
  // KPI Review History
  reviews            KPIReview[]

  @@unique([customerAccountId, code])
  @@index([customerAccountId])
}

// KPI Review History (tracks actual score submissions over time)
model KPIReview {
  id           String          @id @default(cuid())
  reviewDate   DateTime
  actualScore  Float?
  status       String          @default("Scheduled") // Scheduled, Missed, Overdue, Achieved
  documentPath String? // Path to uploaded document
  documentName String? // Name of uploaded document
  kpiId        String
  kpi          KPI             @relation(fields: [kpiId], references: [id], onDelete: Cascade)
  createdAt    DateTime        @default(now())
  updatedAt    DateTime        @updatedAt
  actionPlans  KPIActionPlan[]
}

// KPI Action Plan (for remediation when KPI is missed)
model KPIActionPlan {
  id                  String    @id @default(cuid())
  plannedAction       String // Required: The planned action to take
  description         String? // Optional: Detailed description
  percentageCompleted Float     @default(0) // Required: 0-100
  startDate           DateTime? // Required: When the action starts
  status              String    @default("In-Progress") // Open, In-Progress, Completed
  kpiReviewId         String
  kpiReview           KPIReview @relation(fields: [kpiReviewId], references: [id], onDelete: Cascade)
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt
}

// Policy Attachments (Governance document files)
model PolicyAttachment {
  id         String   @id @default(cuid())
  fileName   String
  fileType   String? // pdf, docx, xlsx, etc.
  fileSize   Int?
  filePath   String
  uploadedAt DateTime @default(now())
  policyId   String
  policy     Policy   @relation(fields: [policyId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

// Governance Vault Document (Information Security Vault)
model GovernanceVaultDocument {
  id                String                        @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount               @relation(fields: [customerAccountId], references: [id])
  documentCode      String                        // Auto-generated code like VD-001
  fileName          String
  fileType          String?                       // pdf, docx, xlsx, etc.
  fileSize          Int?
  filePath          String
  status            String                        @default("Active") // Active, Archived
  uploadedAt        DateTime                      @default(now())
  createdAt         DateTime                      @default(now())
  updatedAt         DateTime                      @updatedAt
  linkedPolicies    GovernanceVaultDocumentLink[]

  @@unique([customerAccountId, documentCode])
  @@index([customerAccountId])
}

// Junction table for linking vault documents to policies (many-to-many)
model GovernanceVaultDocumentLink {
  id         String                  @id @default(cuid())
  documentId String
  document   GovernanceVaultDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)
  policyId   String
  policy     Policy                  @relation(fields: [policyId], references: [id], onDelete: Cascade)
  linkedAt   DateTime                @default(now())

  @@unique([documentId, policyId])
}

// Evidence Attachments (Artifacts)
model EvidenceAttachment {
  id         String   @id @default(cuid())
  fileName   String
  fileType   String? // pdf, docx, xlsx, png, jpg, etc.
  fileSize   Int?
  filePath   String
  uploadedAt DateTime @default(now())
  evidenceId String
  evidence   Evidence @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

// Policy-Control Many-to-Many Junction Table
model PolicyControl {
  id        String   @id @default(cuid())
  policyId  String
  policy    Policy   @relation(fields: [policyId], references: [id], onDelete: Cascade)
  controlId String
  control   Control  @relation(fields: [controlId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([policyId, controlId])
}

// Control-Risk Many-to-Many Junction Table
model ControlRisk {
  id        String   @id @default(cuid())
  controlId String
  control   Control  @relation(fields: [controlId], references: [id], onDelete: Cascade)
  riskId    String
  risk      Risk     @relation(fields: [riskId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([controlId, riskId])
}

// Policy-Exception Many-to-Many Junction Table
model PolicyException {
  id          String    @id @default(cuid())
  policyId    String
  policy      Policy    @relation(fields: [policyId], references: [id], onDelete: Cascade)
  exceptionId String
  exception   Exception @relation(fields: [exceptionId], references: [id], onDelete: Cascade)
  createdAt   DateTime  @default(now())

  @@unique([policyId, exceptionId])
}

// ==================== ASSET MANAGEMENT MODULE ====================

// Asset Category (Top-level categories: Hardware, Software, Data, etc.)
model AssetCategory {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?   @relation(fields: [customerAccountId], references: [id])
  name              String
  description       String?
  status            String             @default("Active") // Active, Inactive
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  subCategories     AssetSubCategory[]
  assets            Asset[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Asset Sub Category (e.g., Server, Firewall under Hardware)
model AssetSubCategory {
  id                      String                   @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId       String?
  customerAccount         CustomerAccount?         @relation(fields: [customerAccountId], references: [id])
  name                    String
  description             String?
  categoryId              String
  category                AssetCategory            @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  status                  String                   @default("Active") // Active, Inactive
  createdAt               DateTime                 @default(now())
  updatedAt               DateTime                 @updatedAt
  assets                  Asset[]
  assetCIAClassifications AssetCIAClassification[]
  groups                  AssetGroup[]

  @@unique([customerAccountId, name, categoryId])
  @@index([customerAccountId])
}

// Asset Group (Logical groupings: Security Tools, Payment Systems, etc.)
model AssetGroup {
  id                      String                   @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId       String?
  customerAccount         CustomerAccount?         @relation(fields: [customerAccountId], references: [id])
  name                    String
  description             String?
  status                  String                   @default("Active") // Active, Inactive
  subCategoryId           String?
  subCategory             AssetSubCategory?        @relation(fields: [subCategoryId], references: [id], onDelete: SetNull)
  createdAt               DateTime                 @default(now())
  updatedAt               DateTime                 @updatedAt
  assets                  Asset[]
  assetCIAClassifications AssetCIAClassification[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
  @@index([subCategoryId])
}

// CIA Rating Levels (for Confidentiality, Integrity, Availability)
model CIARating {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  type              String           // Confidentiality, Integrity, Availability
  label             String           // high, medium, low
  value             Int              // Score value (e.g., 10, 5, 1)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, type, label])
  @@index([customerAccountId])
}

// Asset Sensitivity Levels
model AssetSensitivity {
  id                String                    @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?          @relation(fields: [customerAccountId], references: [id])
  name              String                    // Public, Internal, Confidential, Restricted
  description       String?
  createdAt         DateTime                  @default(now())
  updatedAt         DateTime                  @updatedAt
  assets            Asset[]
  classifications   AssetCIAClassification[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Asset CIA Classification (Links Sub Category + Group with CIA ratings)
model AssetCIAClassification {
  id                    String            @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String?
  customerAccount       CustomerAccount?  @relation(fields: [customerAccountId], references: [id])
  subCategoryId         String
  subCategory           AssetSubCategory  @relation(fields: [subCategoryId], references: [id], onDelete: Cascade)
  groupId               String
  group                 AssetGroup        @relation(fields: [groupId], references: [id], onDelete: Cascade)
  sensitivityId         String?
  sensitivity           AssetSensitivity? @relation(fields: [sensitivityId], references: [id], onDelete: SetNull)
  confidentiality       String            @default("") // high, medium, low
  confidentialityScore  Int               @default(0)
  integrity             String            @default("") // high, medium, low
  integrityScore        Int               @default(0)
  availability          String            @default("") // high, medium, low
  availabilityScore     Int               @default(0)
  assetCriticality      String            @default("low") // high, medium, low
  assetCriticalityScore Int               @default(1)
  createdAt             DateTime          @default(now())
  updatedAt             DateTime          @updatedAt

  @@index([customerAccountId])
  @@index([sensitivityId])
}

// Asset Scoring Configuration (stores the calculation type setting)
model AssetScoringConfig {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  calculationType   String           @default("high_of_all") // high_of_all, addition_of_all, product_of_all
  isActive          Boolean          @default(true)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId])
  @@index([customerAccountId])
}

// Asset Scoring Range (defines criticality levels based on scores for each calculation type)
model AssetScoringRange {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  calculationType   String           // high_of_all, addition_of_all, product_of_all
  level             String           // Critical, High, Medium, Low
  minScore          Int              @default(0)
  maxScore          Int              @default(0)
  color             String?          @default("#000000")
  sortOrder         Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, calculationType, level])
  @@index([customerAccountId])
  @@index([calculationType])
}

// Asset Lifecycle Status
model AssetLifecycleStatus {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String           // Active, In Use, Needs Maintenance, Retired
  description       String?
  order             Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  assets            Asset[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Asset Classification (Legacy - keeping for backwards compatibility)
model AssetClassification {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String           // Critical, High, Medium, Low
  description       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  assets            Asset[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Assets
model Asset {
  id                String                @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount       @relation(fields: [customerAccountId], references: [id])
  assetId           String
  name              String
  description       String?
  // Category hierarchy
  categoryId        String?
  category          AssetCategory?        @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  subCategoryId     String?
  subCategory       AssetSubCategory?     @relation(fields: [subCategoryId], references: [id], onDelete: SetNull)
  groupId           String?
  group             AssetGroup?           @relation(fields: [groupId], references: [id], onDelete: SetNull)
  // Legacy asset type (keeping for backwards compatibility)
  assetType         String?               // Hardware, Software, Information, People, Services, Facility
  // Department and ownership
  departmentId      String?
  department        Department?           @relation(fields: [departmentId], references: [id])
  ownerId           String?
  owner             User?                 @relation("AssetOwner", fields: [ownerId], references: [id])
  custodianId       String?
  custodian         User?                 @relation("AssetCustodian", fields: [custodianId], references: [id])
  // Classification
  classificationId  String?
  classification    AssetClassification?  @relation(fields: [classificationId], references: [id])
  sensitivityId     String?
  sensitivity       AssetSensitivity?     @relation(fields: [sensitivityId], references: [id])
  // Lifecycle
  lifecycleStatusId String?
  lifecycleStatus   AssetLifecycleStatus? @relation(fields: [lifecycleStatusId], references: [id])
  status            String                @default("Active") // Legacy status field
  // Asset details
  value             Float?
  location          String?
  acquisitionDate   DateTime?
  nextReviewDate    DateTime?
  // Timestamps
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt
  // Risks impacted by this asset
  impactedByRisks   Risk[]                @relation("RiskImpactedAsset")
  // Processes that depend on this asset
  processes         Process[]             @relation("ProcessAsset")

  @@unique([customerAccountId, assetId])
  @@index([customerAccountId])
}

// ==================== RISK MANAGEMENT MODULE ====================

// Risk Categories
model RiskCategory {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String           // Strategic, Operational, Financial, Compliance, IT/Cyber, Reputational
  description       String?
  color             String?          // Color code for UI display
  status            String           @default("Active") // Active, Inactive
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  risks             Risk[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Risk Types
model RiskType {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String           // Inherent, Residual, Target
  description       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  risks             Risk[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Risk Threats
model RiskThreat {
  id                String              @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?    @relation(fields: [customerAccountId], references: [id])
  threatId          String?
  name              String
  description       String?
  categoryId        String?
  category          ThreatCategory?     @relation(fields: [categoryId], references: [id])
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  risks             RiskThreatMapping[]

  @@unique([customerAccountId, name])
  @@unique([customerAccountId, threatId])
  @@index([customerAccountId])
}

// Risk Vulnerabilities
model RiskVulnerability {
  id                String                     @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?           @relation(fields: [customerAccountId], references: [id])
  vulnId            String?
  name              String
  description       String?
  categoryId        String?
  category          VulnerabilityCategory?     @relation(fields: [categoryId], references: [id])
  createdAt         DateTime                   @default(now())
  updatedAt         DateTime                   @updatedAt
  risks             RiskVulnerabilityMapping[]

  @@unique([customerAccountId, name])
  @@unique([customerAccountId, vulnId])
  @@index([customerAccountId])
}

// Risk Causes
model RiskCause {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?   @relation(fields: [customerAccountId], references: [id])
  name              String
  description       String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  risks             RiskCauseMapping[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Risk-Threat Mapping (many-to-many)
model RiskThreatMapping {
  id        String     @id @default(cuid())
  riskId    String
  risk      Risk       @relation(fields: [riskId], references: [id], onDelete: Cascade)
  threatId  String
  threat    RiskThreat @relation(fields: [threatId], references: [id], onDelete: Cascade)
  createdAt DateTime   @default(now())

  @@unique([riskId, threatId])
}

// Risk-Vulnerability Mapping (many-to-many)
model RiskVulnerabilityMapping {
  id              String            @id @default(cuid())
  riskId          String
  risk            Risk              @relation(fields: [riskId], references: [id], onDelete: Cascade)
  vulnerabilityId String
  vulnerability   RiskVulnerability @relation(fields: [vulnerabilityId], references: [id], onDelete: Cascade)
  createdAt       DateTime          @default(now())

  @@unique([riskId, vulnerabilityId])
}

// Risk-Cause Mapping (many-to-many)
model RiskCauseMapping {
  id        String    @id @default(cuid())
  riskId    String
  risk      Risk      @relation(fields: [riskId], references: [id], onDelete: Cascade)
  causeId   String
  cause     RiskCause @relation(fields: [causeId], references: [id], onDelete: Cascade)
  createdAt DateTime  @default(now())

  @@unique([riskId, causeId])
}

// Risks
model Risk {
  id                  String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id])
  riskId              String
  name                String
  description         String?
  riskSources         String?         // Sources of the risk
  categoryId          String?
  category            RiskCategory?   @relation(fields: [categoryId], references: [id])
  typeId              String?
  type                RiskType?       @relation(fields: [typeId], references: [id])
  departmentId        String?
  department          Department?     @relation(fields: [departmentId], references: [id])
  ownerId             String?
  owner               User?           @relation("RiskOwner", fields: [ownerId], references: [id])
  // Impacted Asset or Process (based on Risk Type selection)
  impactedAssetId     String?
  impactedAsset       Asset?          @relation("RiskImpactedAsset", fields: [impactedAssetId], references: [id])
  impactedProcessId   String?
  impactedProcess     Process?        @relation("RiskImpactedProcess", fields: [impactedProcessId], references: [id])
  // Assessment scores
  likelihood          Int             @default(1) // 1-5 scale
  impact              Int             @default(1) // 1-5 scale
  riskScore           Int             @default(1) // likelihood * impact
  riskRating          String          @default("Low") // Catastrophic, Very High, High, Medium, Low
  // Inherent, Residual, Target risk scores
  inherentLikelihood  Int?
  inherentImpact      Int?
  inherentRiskScore   Int?
  residualLikelihood  Int?
  residualImpact      Int?
  residualRiskScore   Int?
  targetLikelihood    Int?
  targetImpact        Int?
  targetRiskScore     Int?
  // Response
  status              String          @default("Open") // Awaiting Approval, Pending Assessment, Open, In Progress, Closed
  assessmentStatus    String          @default("Open") // Open, In-Progress, Draft, Submitted, Approved, Rejected, Completed
  responseStatus      String          @default("Open") // Open, In-Progress, Awaiting Approval, Sent Back, Completed (for Risk Response Strategy)
  responseStrategy    String?         // Treat, Transfer, Avoid, Accept
  treatmentPlan       String?
  treatmentDueDate    DateTime?
  treatmentStatus     String?         // Not Started, In Progress, Completed
  // Assessment form data (JSON) - stores per-threat likelihoods, impacts, and per-vulnerability ratings
  assessmentFormData  String?         // JSON: { threatLikelihoods: {}, threatImpacts: {}, vulnerabilityRatings: {} }
  // Timestamps
  identifiedDate      DateTime        @default(now())
  lastAssessmentDate  DateTime?
  nextReviewDate      DateTime?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  // Relations
  threats             RiskThreatMapping[]
  vulnerabilities     RiskVulnerabilityMapping[]
  causes              RiskCauseMapping[]
  assessments         RiskAssessment[]
  responses           RiskResponse[]
  activityLogs        RiskActivityLog[]
  controlRisks        ControlRisk[]
  exceptions          Exception[]     @relation("RiskExceptions")
  // Risk Control Matrix entries (independent, deleting entry doesn't delete this risk)
  riskControlMatrixEntries RiskControlMatrixEntry[]

  @@unique([customerAccountId, riskId])
  @@index([customerAccountId])
}

// Risk Control Matrix Entry - Independent entries for Risk Control Matrix view
// Deleting these entries does NOT delete the underlying Risk
model RiskControlMatrixEntry {
  id                  String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id])
  // Reference to the source Risk (optional - allows matrix entry to exist even if risk is deleted)
  riskId              String?
  risk                Risk?           @relation(fields: [riskId], references: [id], onDelete: SetNull)
  // Matrix entry identifier
  matrixEntryId       String          // e.g., "RCM-001"
  // Snapshot of risk data (so matrix entry is independent)
  riskCode            String          // Copy of risk.riskId
  name                String
  description         String?
  riskRating          String?
  residualRiskRating  String?
  status              String          @default("Open")
  ownerName           String?
  // Linked controls through junction table
  linkedControls      RiskControlMatrixControl[]
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt

  @@unique([customerAccountId, matrixEntryId])
  @@index([customerAccountId])
}

// Junction table for Risk Control Matrix Entry to Control
model RiskControlMatrixControl {
  id                        String                  @id @default(cuid())
  riskControlMatrixEntryId  String
  riskControlMatrixEntry    RiskControlMatrixEntry  @relation(fields: [riskControlMatrixEntryId], references: [id], onDelete: Cascade)
  controlId                 String
  control                   Control                 @relation(fields: [controlId], references: [id], onDelete: Cascade)
  createdAt                 DateTime                @default(now())

  @@unique([riskControlMatrixEntryId, controlId])
}

// Risk Assessment Records
model RiskAssessment {
  id                        String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId         String
  customerAccount           CustomerAccount @relation(fields: [customerAccountId], references: [id])
  assessmentId              String
  riskId                    String
  risk                      Risk            @relation(fields: [riskId], references: [id], onDelete: Cascade)
  assessmentType            String          @default("Periodic") // Initial, Periodic, Ad-hoc
  assessorName              String?
  // Assessment scores
  likelihood                Int             @default(1)
  likelihoodRationale       String?
  impact                    Int             @default(1)
  impactRationale           String?
  riskScore                 Int             @default(1)
  riskRating                String          @default("Low")
  // Identified items during assessment
  threatsIdentified         String?  // JSON array of threat names
  vulnerabilitiesIdentified String? // JSON array of vulnerability names
  causesIdentified          String?  // JSON array of cause names
  // Recommendations
  recommendations           String?
  notes                     String?
  // Status
  status                    String          @default("Draft") // Draft, Submitted, Approved, Rejected
  assessmentDate            DateTime        @default(now())
  createdAt                 DateTime        @default(now())
  updatedAt                 DateTime        @updatedAt

  @@unique([customerAccountId, assessmentId])
  @@index([customerAccountId])
}

// Risk Response Records
model RiskResponse {
  id                  String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id])
  responseId          String
  riskId              String
  risk                Risk            @relation(fields: [riskId], references: [id], onDelete: Cascade)
  responseType        String          @default("Mitigate") // Mitigate, Transfer, Accept, Avoid
  actionTitle         String
  actionDescription   String?
  assignee            String?
  dueDate             DateTime?
  status              String          @default("Open") // Open, In Progress, Completed, Overdue
  completionDate      DateTime?
  effectivenessRating Int?   // 1-5 scale
  notes               String?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt

  @@unique([customerAccountId, responseId])
  @@index([customerAccountId])
}

// Risk Settings (configurable parameters)
model RiskSetting {
  id          String   @id @default(cuid())
  category    String   // likelihood_scale, impact_scale, rating_matrix, appetite, frequency
  key         String
  value       String   // JSON value
  label       String?
  description String?
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([category, key])
}

// ==================== RISK SETTINGS MASTER DATA ====================

// Vulnerability Category (for categorizing vulnerabilities)
model VulnerabilityCategory {
  id                String             @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount?   @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  vulnerabilities   RiskVulnerability[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Threat Category (for categorizing threats)
model ThreatCategory {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  threats           RiskThreat[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Control Strength (effectiveness rating of controls)
model ControlStrength {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  score             Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Risk Likelihood (probability scale)
model RiskLikelihood {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  title             String
  score             Int              @default(0)
  timeFrame         String?          // e.g., "Within 1 year", "Within 5 years"
  probability       String?          // e.g., ">90%", "50-90%", "<10%"
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, title])
  @@index([customerAccountId])
}

// Impact Category (types of impact)
model ImpactCategory {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Impact Rating (severity scale)
model ImpactRating {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  score             Int              @default(0)
  description       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Vulnerability Rating (severity of vulnerabilities)
model VulnerabilityRating {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  label             String
  score             Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, label])
  @@index([customerAccountId])
}

// Risk Sub Category
model RiskSubCategory {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  type              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, type])
  @@index([customerAccountId])
}

// Risk Range (risk rating thresholds with colors and timelines)
model RiskRange {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  title             String
  color             String?          // Hex color code
  lowRange          Int              @default(0)
  highRange         Int              @default(0)
  timelineDays      Int              @default(0) // Days to remediate
  description       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, title])
  @@index([customerAccountId])
}

// Risk Score Configuration (formula settings)
model RiskScoreConfig {
  id                    String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId     String?
  customerAccount       CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  useLikelihood         Boolean          @default(true)
  useImpact             Boolean          @default(true)
  useAssetScore         Boolean          @default(false)
  useVulnerabilityScore Boolean          @default(false)
  riskTolerance         Int              @default(10)
  createdAt             DateTime         @default(now())
  updatedAt             DateTime         @updatedAt

  @@index([customerAccountId])
}

// ==================== END RISK SETTINGS MASTER DATA ====================

// Risk Activity Log (for tracking all risk-related activities)
model RiskActivityLog {
  id          String   @id @default(cuid())
  riskId      String
  risk        Risk     @relation(fields: [riskId], references: [id], onDelete: Cascade)
  activity    String   // Created, Updated, Deleted, Status Changed, Assessment Added, etc.
  description String?  // Detailed description of what changed
  actor       String   // User who performed the action
  actorId     String?  // User ID if available
  metadata    String?  // JSON string for additional data
  createdAt   DateTime @default(now())
}

// ==================== INTERNAL AUDIT MODULE ====================

// Legacy Audits (keeping for backwards compatibility)
model Audit {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  auditId           String
  name              String
  description       String?
  auditType         String? // Internal, External
  departmentId      String?
  department        Department?     @relation(fields: [departmentId], references: [id])
  auditorId         String?
  auditor           User?           @relation("Auditor", fields: [auditorId], references: [id])
  status            String          @default("Planned") // Planned, In Progress, Completed, Cancelled
  startDate         DateTime?
  endDate           DateTime?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  findings          AuditFinding[]

  @@unique([customerAccountId, auditId])
  @@index([customerAccountId])
}

// Audit Findings
model AuditFinding {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  findingId         String
  title             String
  description       String?
  auditId           String?
  audit             Audit?          @relation(fields: [auditId], references: [id])
  severity          String          @default("Medium") // High, Medium, Low
  status            String          @default("Open") // Open, In Progress, Closed
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  capas             CAPA[]

  @@unique([customerAccountId, findingId])
  @@index([customerAccountId])
}

// CAPA (Corrective and Preventive Actions)
model CAPA {
  id                String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id])
  capaId            String
  title             String
  description       String?
  findingId         String?
  finding           AuditFinding?   @relation(fields: [findingId], references: [id])
  actionType        String          @default("Corrective") // Corrective, Preventive
  status            String          @default("Open") // Open, In Progress, Closed, Overdue
  dueDate           DateTime?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, capaId])
  @@index([customerAccountId])
}

// ==================== INTERNAL AUDIT SETTINGS ====================

// Audit Category (e.g., Financial Audit, Operational Audit, Compliance Audit)
model AuditCategory {
  id                  String              @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String?
  customerAccount     CustomerAccount?    @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId         String?
  auditHead           User?               @relation("AuditHeadCategories", fields: [auditHeadId], references: [id])
  name                String
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt
  internalAuditRisks  InternalAuditRisk[]

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Nature of Controls (e.g., Preventive, Detective, Corrective)
model AuditNatureOfControl {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadNatureOfControls", fields: [auditHeadId], references: [id])
  label             String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, label])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Risk Factors for Internal Audit (e.g., Financial, Reputational, Regulatory)
model AuditRiskFactor {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadRiskFactors", fields: [auditHeadId], references: [id])
  label             String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, label])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Probability ratings (e.g., Very Low=1, Low=2, Medium=3, High=4, Very High=5)
model AuditProbability {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadProbabilities", fields: [auditHeadId], references: [id])
  label             String
  value             Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, label])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Impact ratings (e.g., Very Low Probability=1, Low Probability=2, etc.)
model AuditImpact {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadImpacts", fields: [auditHeadId], references: [id])
  label             String
  value             Int              @default(0)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, label])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Scoring Range Configuration (e.g., High: 250+, Medium: 100-249, Low: 50-99)
model AuditScoringRange {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadScoringRanges", fields: [auditHeadId], references: [id])
  label             String
  lowValue          Int              @default(0)
  highValue         Int?
  calculationType   String           @default("High of all") // High of all, Addition of all, Product of all
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, label, calculationType])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Audit Scoring Configuration (stores the calculation type settings)
model AuditScoringConfig {
  id                        String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId         String?
  customerAccount           CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId               String?
  auditHead                 User?            @relation("AuditHeadScoringConfigs", fields: [auditHeadId], references: [id])
  probabilityImpactCalcType String           @default("Product of all") // High of all, Addition of all, Product of all
  riskRatingCalcType        String           @default("High of all") // High of all, Addition of all, Product of all
  createdAt                 DateTime         @default(now())
  updatedAt                 DateTime         @updatedAt

  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Periodicity (e.g., Annual=12, Semiannual=6, Monthly=1)
model AuditPeriodicity {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadPeriodicities", fields: [auditHeadId], references: [id])
  interval          String
  months            Int              @default(1)
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, interval])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Escalation Configuration (reminder days for various events)
model AuditEscalationConfig {
  id                  String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String?
  customerAccount     CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId         String?
  auditHead           User?            @relation("AuditHeadEscalationConfigs", fields: [auditHeadId], references: [id])
  responseSubmission  Int              @default(5)
  acknowledgement     Int              @default(1)
  clarification       Int              @default(2)
  issueResolution     Int              @default(3)
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt

  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Audit Type (e.g., Consult, Assurance, Follow-up)
model AuditType {
  id                  String              @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String?
  customerAccount     CustomerAccount?    @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId         String?
  auditHead           User?               @relation("AuditHeadTypes", fields: [auditHeadId], references: [id])
  name                String
  createdAt           DateTime            @default(now())
  updatedAt           DateTime            @updatedAt
  internalAuditRisks  InternalAuditRisk[]
  engagements         AuditEngagement[]   @relation("EngagementAuditType")

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Location (e.g., Head Office, Branch A, Remote)
model AuditLocation {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadLocations", fields: [auditHeadId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Process Frequency (e.g., Daily, Weekly, Monthly, Quarterly, Annually)
model ProcessFrequency {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadProcessFrequencies", fields: [auditHeadId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Nature of Implementation (e.g., Manual, Automated, Manual + Automated)
model NatureOfImplementation {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadNatureOfImplementations", fields: [auditHeadId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// Organization Location (for organization settings location options)
model OrganizationLocation {
  id                String           @id @default(cuid())
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

model Designation {
  id                String           @id @default(cuid())
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

model UserDocumentType {
  id                String           @id @default(cuid())
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// Internal Audit Process (for audit head specific process settings)
model InternalAuditProcess {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: each Audit Head has their own settings
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadProcesses", fields: [auditHeadId], references: [id])
  name              String
  description       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([customerAccountId, auditHeadId, name])
  @@index([customerAccountId])
  @@index([auditHeadId])
}

// ==================== INTERNAL AUDIT RISK REGISTER ====================

// Internal Audit Risk (separate from general Risk for the Internal Audit module)
model InternalAuditRisk {
  id                   String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId    String
  customerAccount      CustomerAccount  @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation
  auditHeadId          String?
  auditHead            User?            @relation("AuditHeadRisks", fields: [auditHeadId], references: [id])
  riskId               String // Auto-generated RID001, RID002, etc.
  riskName             String
  departmentId         String?
  department           Department?      @relation("InternalAuditDepartment", fields: [departmentId], references: [id])
  sectionProcess       String?
  subProcess           String?
  activity             String?
  categoryId           String?
  category             AuditCategory?   @relation(fields: [categoryId], references: [id])
  auditTypeId          String?
  auditType            AuditType?       @relation(fields: [auditTypeId], references: [id])
  riskDescription      String?
  // Inherent Risk Scoring
  inherentLikelihood   Int?
  inherentImpact       Int?
  inherentScore        Int?             // Calculated: likelihood * impact or based on config
  // Controls
  controlDescription   String?
  controlEffectiveness String?          // Effective, Partially Effective, Ineffective
  // Residual Risk Scoring
  residualLikelihood   Int?
  residualImpact       Int?
  residualScore        Int?             // Calculated
  riskLevel            String?          // Low, Medium, High, Extreme - based on scoring ranges
  // Audit Information
  creationDate         DateTime         @default(now())
  auditComment         String?
  status               String           @default("Open") // Open, Closed, Under Review
  evidenceFilePath     String?
  evidenceFileName     String?
  // Engagement link
  engagementId         String?
  engagement           AuditEngagement? @relation(fields: [engagementId], references: [id])
  createdAt            DateTime         @default(now())
  updatedAt            DateTime         @updatedAt

  @@unique([customerAccountId, riskId])
  @@index([customerAccountId])
}

// ==================== INTERNAL AUDIT - AUDIT UNIVERSE ====================

// Auditable Entity (items within departments that can be audited)
model AuditableEntity {
  id                String            @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount   @relation(fields: [customerAccountId], references: [id])
  entityCode        String
  name              String
  description       String?
  departmentId      String
  department        Department        @relation(fields: [departmentId], references: [id], onDelete: Cascade)
  plannedHours      Float             @default(0)
  actualHours       Float             @default(0)
  lastAuditDate     DateTime?
  nextAuditDate     DateTime?
  riskRating        String?           // Low, Medium, High
  status            String            @default("Active") // Active, Inactive
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  engagements       AuditEngagement[]

  @@unique([customerAccountId, entityCode])
  @@index([customerAccountId])
}

// ==================== INTERNAL AUDIT - AUDIT PLANNING ====================

// Audit Engagement (Annual Audit Plan items)
model AuditEngagement {
  id                  String            @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount   @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation: Link to the audit head who owns this engagement
  auditHeadId         String?
  auditHead           User?             @relation("AuditHeadEngagements", fields: [auditHeadId], references: [id])
  auditId             String // Auto-generated AUD001, AUD002
  engagementTitle     String
  engagementObjective String?
  engagementScope     String?
  description         String?
  departmentId        String?
  department          Department?       @relation(fields: [departmentId], references: [id])
  auditableEntityId   String?
  auditableEntity     AuditableEntity?  @relation(fields: [auditableEntityId], references: [id])
  auditTypeId         String?
  auditType           String?           // Internal Audit, Compliance, Financial, etc.
  auditTypeRef        AuditType?        @relation("EngagementAuditType", fields: [auditTypeId], references: [id])
  auditRating         String?           // Satisfactory, Needs Improvement, Unsatisfactory
  assignedAuditorId   String?
  assignedAuditor     User?             @relation("EngagementAuditor", fields: [assignedAuditorId], references: [id])
  auditeeId           String?
  auditee             User?             @relation("EngagementAuditee", fields: [auditeeId], references: [id])
  teamMembers         User[]            @relation("EngagementTeamMembers")
  // Planning dates
  plannedStartDate    DateTime?
  plannedEndDate      DateTime?
  actualStartDate     DateTime?
  actualEndDate       DateTime?
  startDate           DateTime?
  endDate             DateTime?
  // Hours
  plannedHours        Float             @default(0)
  actualHours         Float             @default(0)
  // Additional fields
  initialObservation  String?
  relatedPolicies     String?
  // Process relation
  processId           String?
  process             Process?          @relation("EngagementProcess", fields: [processId], references: [id])
  // Status
  status              String            @default("Planned") // Planned, In Progress, Completed, Cancelled
  priority            String            @default("Medium") // Low, Medium, High
  year                Int               @default(2026)
  quarter             String?           // Q1, Q2, Q3, Q4
  // Timestamps
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  // Relations
  fieldwork           AuditFieldwork?
  report              AuditReport?
  findings            InternalAuditFinding[]
  evidenceRequests    FieldworkEvidenceRequest[]
  linkedRisks         InternalAuditRisk[]
  aiWorkpapers        AiWorkpaper[]
  tasks               AuditEngagementTask[]
  comments            EngagementComment[]

  @@unique([customerAccountId, auditId])
  @@index([customerAccountId])
}

// Engagement Comments
model EngagementComment {
  id            String          @id @default(cuid())
  engagementId  String
  engagement    AuditEngagement @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  userId        String
  user          User            @relation("EngagementComments", fields: [userId], references: [id])
  comment       String
  createdAt     DateTime        @default(now())

  @@index([engagementId])
}

// ==================== INTERNAL AUDIT - FIELDWORK ====================

// Audit Fieldwork (execution of audit engagement)
model AuditFieldwork {
  id              String          @id @default(cuid())
  engagementId    String          @unique
  engagement      AuditEngagement @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  // Execution details
  startDate       DateTime?
  targetDate      DateTime?
  completionDate  DateTime?
  // Status
  status          String          @default("Not Started") // Not Started, In Progress, Review, Completed
  // Work details
  scope           String?
  objectives      String?
  methodology     String?
  observations    String?
  // Hours tracking
  hoursSpent      Float           @default(0)
  // Timestamps
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
  workpapers      AuditWorkpaper[]
}

// Audit Workpapers (supporting documents for fieldwork)
model AuditWorkpaper {
  id           String         @id @default(cuid())
  fieldworkId  String
  fieldwork    AuditFieldwork @relation(fields: [fieldworkId], references: [id], onDelete: Cascade)
  fileName     String
  fileType     String?
  fileSize     Int?
  filePath     String
  description  String?
  uploadedBy   String?
  uploadedAt   DateTime       @default(now())
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
}

// AI-Generated Workpapers (structured audit tasks from AI)
model AiWorkpaper {
  id                String          @id @default(cuid())
  engagementId      String
  engagement        AuditEngagement @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  task              String          // Task name from AI
  steps             String          // Audit steps (JSON array as string)
  evidences         String          // Evidence to collect (JSON array as string)
  questionChecklist String          // Checklist questions (JSON array as string)
  comments          String?         // Comments/notes
  executed          Boolean         @default(false) // Whether task has been executed
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([engagementId])
  @@map("AIWorkpaper")
}

// Audit Engagement Task List (manual audit tasks for fieldwork)
model AuditEngagementTask {
  id              String          @id @default(cuid())
  engagementId    String
  engagement      AuditEngagement @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  refNo           Int             // Sequential reference number within engagement
  task            String          @default("") // Task description
  document        String?         // File path for attached document
  documentName    String?         // Original filename of attached document
  executed        Boolean         @default(false) // Whether task has been executed
  comments        String          @default("") // Task comments
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt

  @@index([engagementId])
}

// Fieldwork Evidence Request (requests for evidence during audit fieldwork)
model FieldworkEvidenceRequest {
  id              String                         @id @default(cuid())
  engagementId    String
  engagement      AuditEngagement                @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  title           String
  description     String?
  sampleSize      String?
  status          String                         @default("Pending") // Pending, In Progress, Submitted, Reviewed
  aiReviewStatus  String?                        // Satisfactory, Needs Attention
  aiReviewComment String?
  dueDate         DateTime?
  category        String?                        // workpapers, other, evidence-request
  documentType    String?                        // Policy, Procedure, Evidence, Report, etc.
  // Auditee assignment (assigned by Audit Head)
  auditeeId       String?
  auditeeName     String?
  // Clarification request fields (sent by Audit Head to Auditee)
  clarificationComment      String?
  clarificationDocumentName String?
  clarificationByUserId     String?
  clarificationByUserName   String?
  clarificationSentAt       DateTime?
  createdAt       DateTime                       @default(now())
  updatedAt       DateTime                       @updatedAt
  attachments     FieldworkEvidenceAttachment[]
}

// Fieldwork Evidence Attachment (files attached to evidence requests)
model FieldworkEvidenceAttachment {
  id                String                   @id @default(cuid())
  evidenceRequestId String
  evidenceRequest   FieldworkEvidenceRequest @relation(fields: [evidenceRequestId], references: [id], onDelete: Cascade)
  fileName          String
  fileType          String?
  fileSize          Int?
  filePath          String
  uploadedBy        String?
  uploadedAt        DateTime                 @default(now())
  createdAt         DateTime                 @default(now())
  updatedAt         DateTime                 @updatedAt
}

// ==================== INTERNAL AUDIT - REPORTS ====================

// Audit Report
model AuditReport {
  id                  String          @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation
  auditHeadId         String?
  auditHead           User?           @relation("AuditHeadReports", fields: [auditHeadId], references: [id])
  reportCode          String // Auto-generated RPT001, RPT002
  engagementId        String          @unique
  engagement          AuditEngagement @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  // Report content
  title               String
  executiveSummary    String?
  scope               String?
  objectives          String?
  methodology         String?
  observations        String?
  recommendations     String?
  managementResponse  String?
  conclusion          String?
  // Overall Result (Pass/Fail)
  overallResult       String?         // Pass, Fail
  // Status
  status              String          @default("Draft") // Draft, Review, Final, Published
  draftGeneratedAt    DateTime?
  reviewedAt          DateTime?
  publishedAt         DateTime?
  reviewedBy          String?
  // Auditee assignment (assigned by Audit Head)
  auditeeId           String?
  auditeeName         String?
  auditeeComment      String?
  // Attachment
  reportFilePath      String?
  reportFileName      String?
  // Timestamps
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt

  @@unique([customerAccountId, reportCode])
  @@index([customerAccountId])
}

// ==================== INTERNAL AUDIT - FINDINGS & CAPA ====================

// Internal Audit Finding (linked to engagement, separate from general AuditFinding)
model InternalAuditFinding {
  id                  String                @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId   String
  customerAccount     CustomerAccount       @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation
  auditHeadId         String?
  auditHead           User?                 @relation("AuditHeadFindings", fields: [auditHeadId], references: [id])
  findingId           String // Auto-generated FND001, FND002
  engagementId        String
  engagement          AuditEngagement       @relation(fields: [engagementId], references: [id], onDelete: Cascade)
  // Finding details
  finding             String
  description         String?
  severity            String                @default("Medium") // Low, Medium, High, Critical
  departmentId        String?
  department          Department?           @relation(fields: [departmentId], references: [id])
  responsiblePerson   String?
  responsiblePersonId String?
  // Audit Finding Fields (Criteria, Condition, Cause, Effect)
  criteria            String?               // What should be
  condition           String?               // What is
  cause               String?               // Why it happened
  effect              String?               // The consequence
  recommendation      String?               // Recommendation
  // Dates
  identifiedDate      DateTime              @default(now())
  targetDate          DateTime?
  closedDate          DateTime?
  // Status
  status              String                @default("Open") // Open, In Progress, Closed, Overdue, Under Review
  // AI Review fields
  aiReviewStatus      String?               // "Satisfactory" | "Unsatisfactory" | null
  aiReviewDescription String?               // AI-generated description
  aiReviewedAt        DateTime?             // When AI review was completed
  aiReviewApproved    Boolean               @default(false) // Has Audit Head approved the review?
  aiApprovedAt        DateTime?             // When Audit Head approved
  aiApprovedBy        String?               // Audit Head user ID who approved
  // Timestamps
  createdAt           DateTime              @default(now())
  updatedAt           DateTime              @updatedAt
  // Relations
  capas               InternalAuditCAPA[]
  attachments         FindingAttachment[]

  @@unique([customerAccountId, findingId])
  @@index([customerAccountId])
}

// Finding Attachment (for auditee uploads in CAPA tracking)
model FindingAttachment {
  id          String               @id @default(cuid())
  findingId   String
  finding     InternalAuditFinding @relation(fields: [findingId], references: [id], onDelete: Cascade)
  fileName    String
  fileType    String?
  fileSize    Int?
  filePath    String
  uploadedBy  String?
  uploadedAt  DateTime             @default(now())
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt
}

// Internal Audit CAPA (Corrective and Preventive Actions for internal audit findings)
model InternalAuditCAPA {
  id                String                @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount       @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation
  auditHeadId       String?
  auditHead         User?                 @relation("AuditHeadCAPAs", fields: [auditHeadId], references: [id])
  capaId            String // Auto-generated CAPA001, CAPA002
  findingId         String
  finding           InternalAuditFinding  @relation(fields: [findingId], references: [id], onDelete: Cascade)
  // CAPA details
  title             String
  description       String?
  actionType        String                @default("Corrective") // Corrective, Preventive
  responsiblePerson String?
  // Dates
  targetDate        DateTime?
  completedDate     DateTime?
  // Status
  status            String                @default("Open") // Open, In Progress, Closed, Overdue
  // Evidence
  evidenceFilePath  String?
  evidenceFileName  String?
  // Timestamps
  createdAt         DateTime              @default(now())
  updatedAt         DateTime              @updatedAt

  @@unique([customerAccountId, capaId])
  @@index([customerAccountId])
}

// ==================== INTERNAL AUDIT - DOCUMENT LIBRARY ====================

// Internal Audit Document (for document library)
model InternalAuditDocument {
  id           String   @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  // Audit Head isolation - documents are NOT shared between Audit Heads
  auditHeadId       String?
  auditHead         User?            @relation("AuditHeadDocuments", fields: [auditHeadId], references: [id])
  documentCode String   @unique
  name         String
  description  String?
  category     String   @default("Policy") // Policy, Regulation, PreviousReport
  fileName     String
  fileType     String?
  fileSize     Int?
  filePath     String
  fileData     Bytes?        // Store file binary for serverless (Vercel) compatibility
  uploadedBy   String?
  uploadedAt   DateTime @default(now())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  // AI Ingest jobs for this document
  ingestJobs   DocumentLibraryIngestJob[]
}

// Document Library Search History
model DocumentSearch {
  id        String   @id @default(cuid())
  query     String
  result    String?  // AI response text
  status    String   @default("Satisfactory") // Satisfactory, Unsatisfactory
  userId    String?
  createdAt DateTime @default(now())
}

// Document Library AI Ingest Job - tracks async ingestion jobs for documents
model DocumentLibraryIngestJob {
  id              String    @id @default(cuid())
  documentId      String
  document        InternalAuditDocument @relation(fields: [documentId], references: [id], onDelete: Cascade)
  runpodJobId     String    // RunPod job ID (multiple docs can share one job)
  customerId      String?   // customer_id sent to RunPod
  status          String    @default("queued") // queued, processing, completed, failed
  error           String?   @db.Text
  result          String?   @db.Text // JSON result
  completedAt     DateTime? // When the job completed
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @default(now()) @updatedAt

  @@index([documentId])
  @@index([runpodJobId])
}

// ==================== GOVERNANCE TEMPLATES ====================

// Governance Templates (Policy, Standard, Procedure templates)
model GovernanceTemplate {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String?
  customerAccount   CustomerAccount? @relation(fields: [customerAccountId], references: [id])
  name              String
  governanceType    String           @default("Policy") // Policy, Standard, Procedure
  fileName          String
  fileType          String?          // docx only for AI generation
  fileSize          Int?
  filePath          String
  fileData          Bytes?            // Store file content for cloud deployment
  uploadedById      String?
  uploadedBy        User?            @relation("TemplateUploader", fields: [uploadedById], references: [id])
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
}

// ==================== AUDIT LOGS ====================

model AuditLog {
  id         String   @id @default(cuid())
  changeType String // CREATE, UPDATE, DELETE
  entityType String // e.g., Control, User, Risk
  entityId   String
  userId     String?
  userName   String?
  changes    String? // JSON string of old/new values
  createdAt  DateTime @default(now())
}

// ==================== AI INFRASTRUCTURE ====================

model AIOperation {
  id                String           @id @default(cuid())
  endpoint          String
  method            String           @default("POST")
  requestBody       String?          // Store as JSON string
  responseBody      String?          // Store as JSON string
  statusCode        Int?
  latencyMs         Int?
  error             String?
  jobId             String?          // Link to AIJob if applicable
  userId            String?
  user              User?            @relation(fields: [userId], references: [id])
  evidenceAIReviews EvidenceAIReview[]
  policyAIReviews   PolicyAIReview[]
  createdAt         DateTime         @default(now())

  @@index([userId])
  @@index([jobId])
}

model AIJob {
  id                String           @id @default(cuid())
  type              String           // e.g., "RISK_GENERATION", "SEMANTIC_MATCHING"
  status            String           @default("PENDING") // PENDING, PROCESSING, COMPLETED, FAILED
  providerJobId     String?          @unique // External ID from Runpod/AI Service
  result            String?          // JSON result or reference
  error             String?
  metadata          String?          // JSON metadata
  userId            String?
  user              User?            @relation(fields: [userId], references: [id])
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([userId])
}

model EvidenceAIReview {
  id                String           @id @default(cuid())
  evidenceId        String
  evidence          Evidence         @relation(fields: [evidenceId], references: [id], onDelete: Cascade)

  // Explicit artifact reference for vault-query reviews
  artifactId        String?
  artifact          Artifact?        @relation(fields: [artifactId], references: [id], onDelete: SetNull)

  // Document ID tracking fields
  ingestJobId       String?          // RunPod job ID from artifact ingestion
  evidenceDocumentId   String?       // evidence.id sent as document_id during evidence ingest
  artifactDocumentId   String?       // artifact.id sent as document_id during vault-query ingest
  runpodDocumentRef    String?       // document_id returned by RunPod (if different from what we sent)

  // DEPRECATED: keep for backward compatibility, use ingestJobId instead
  documentId        String?          // External AI document ID (RunPod)

  status            String           @default("pending") // pending, completed, failed
  critique          String?          @db.Text
  complianceSummary String?          @db.Text
  complianceScore   Float?
  gaps              String?          @db.Text // JSON string
  suggestions       String?          @db.Text // JSON string
  similarityScore   Float?
  recommendations   String?          @db.Text // JSON string
  sources           String?          @db.Text // JSON array of sources from vault query
  rawResponse       String?          @db.Text // Full AI response JSON
  aiOperationId     String?
  aiOperation       AIOperation?     @relation(fields: [aiOperationId], references: [id])
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([evidenceId])
  @@index([artifactId])
  @@index([ingestJobId])
}

// Evidence AI Ingest Job - tracks RunPod ingest jobs
model EvidenceAIIngestJob {
  id            String    @id @default(cuid())
  evidenceId    String
  evidence      Evidence  @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  attachmentId  String?   // Optional specific attachment
  runpodJobId   String    @unique // RunPod job ID

  // Document ID tracking: what we sent vs what RunPod returned
  sentDocumentId     String?   // The document_id we sent (evidence.id or artifact.id)
  returnedDocumentId String?   // The document_id RunPod returned (if any)
  ingestedFileName   String?   // Actual file name sent to RunPod (for proper delete matching)

  status        String    @default("queued") // queued, processing, completed, failed
  error         String?   @db.Text
  result        String?   @db.Text // JSON result
  completedAt   DateTime? // When the job completed
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @default(now()) @updatedAt

  @@index([evidenceId])
  @@index([runpodJobId])
  @@index([sentDocumentId])
}

// Evidence AI Ingest Result - stores extracted content from AI ingest
model EvidenceAIIngestResult {
  id              String    @id @default(cuid())
  evidenceId      String
  evidence        Evidence  @relation(fields: [evidenceId], references: [id], onDelete: Cascade)
  jobId           String    @unique // RunPod job ID reference
  extractedText   String?   @db.Text // JSON array of extracted messages
  embeddings      String?   @db.Text // Optional embeddings data
  metadata        String?   @db.Text // JSON metadata
  indexingStatus  String?   // success, failed
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @default(now()) @updatedAt

  @@index([evidenceId])
  @@index([jobId])
}

model PolicyAIReview {
  id                String           @id @default(cuid())
  policyId          String
  policy            Policy           @relation(fields: [policyId], references: [id], onDelete: Cascade)
  documentId        String?          // External AI document ID
  status            String           @default("ingested") // ingested, completed, failed
  complianceSummary String?          @db.Text
  riskScore         Float?
  matchedControls   String?          @db.Text // JSON
  gaps              String?          @db.Text // JSON
  recommendations   String?          @db.Text // JSON
  reviewedAt        DateTime?
  aiOperationId     String?
  aiOperation       AIOperation?     @relation(fields: [aiOperationId], references: [id])
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([policyId])
}

// ==================== IN-APP NOTIFICATIONS ====================

// Notification Types for categorizing notifications
// Types: EVIDENCE_DUE, RISK_ASSIGNED, AUDIT_FINDING, CAPA_DUE, CONTROL_REVIEW,
//        ENGAGEMENT_ASSIGNED, APPROVAL_REQUIRED, SYSTEM, etc.

model Notification {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount  @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  // Recipient
  userId            String
  user              User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  // Notification content
  type              String           // e.g., EVIDENCE_DUE, RISK_ASSIGNED, AUDIT_FINDING, etc.
  title             String
  message           String           @db.Text
  // Optional link to related entity
  relatedEntityType String?          // e.g., "evidence", "risk", "audit", "capa", "control"
  relatedEntityId   String?          // ID of the related entity
  link              String?          // URL to navigate when clicked
  // Notification state
  priority          String           @default("normal") // low, normal, high, urgent
  isRead            Boolean          @default(false)
  readAt            DateTime?
  // Metadata
  metadata          String?          @db.Text // JSON for additional data
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([userId, isRead])
  @@index([customerAccountId])
  @@index([createdAt])
}

// User notification preferences
model NotificationPreference {
  id                String           @id @default(cuid())
  // Multi-tenant: Link to customer account
  customerAccountId String
  customerAccount   CustomerAccount  @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  // User
  userId            String
  user              User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  // Preference settings
  notificationType  String           // e.g., EVIDENCE_DUE, RISK_ASSIGNED, etc. or "ALL"
  inAppEnabled      Boolean          @default(true)
  emailEnabled      Boolean          @default(false)
  // Email frequency (immediate, daily_digest, weekly_digest)
  emailFrequency    String           @default("immediate")
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@unique([userId, notificationType])
  @@index([customerAccountId])
}

// ==================== EMAIL CONFIGURATION (GLOBAL - INSTANCE LEVEL) ====================
// Email settings are global for the entire application instance.
// Managed by GRCAdministrator (superadmin). All customers use the same SMTP config.

// EmailSettings - Global SMTP configuration (only ONE record should exist)
model EmailSettings {
  id                String           @id @default(cuid())
  // SMTP Configuration
  smtpHost          String           // e.g., "smtp.office365.com"
  smtpPort          Int              @default(587)
  smtpUser          String           // SMTP username/email
  smtpPassword      String           // SMTP password (should be encrypted in production)
  fromAddress       String           // e.g., "notifications@company.com"
  fromName          String?          // Display name for From field, e.g., "GRC System"
  replyToAddress    String?          // Reply-to address
  // Security settings
  useTLS            Boolean          @default(true)
  useSSL            Boolean          @default(false) // SSL on port 465
  // Status
  isActive          Boolean          @default(true)
  isVerified        Boolean          @default(false) // Set to true after successful test email
  lastTestedAt      DateTime?
  lastTestResult    String?          // Success/Error message from last test
  // Metadata
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
}

// EmailTemplate - Global email templates (managed by GRCAdministrator)
model EmailTemplate {
  id                String           @id @default(cuid())
  // Template identification
  code              String           @unique // Unique code, e.g., "EVIDENCE_ASSIGNED"
  name              String           // Human-readable name
  description       String?          // Description of when this template is used
  // Template content
  subject           String           // Email subject with {placeholders}
  bodyHtml          String           @db.Text // HTML body with {placeholders}
  bodyText          String?          @db.Text // Plain text alternative
  // Available placeholders (stored as JSON array)
  // e.g., ["recipientName", "entityName", "entityLink", "dueDate", "actorName"]
  placeholders      String?          @db.Text
  // Template settings
  category          String           @default("notification") // notification, reminder, system, custom
  isActive          Boolean          @default(true)
  isSystem          Boolean          @default(false) // System templates cannot be deleted
  module            String           @default("grc") // "grc" or "tprm"
  // Metadata
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt

  @@index([code])
  @@index([module])
}

// ==================== TPRM (Third-Party Risk Management) ====================

// TPRMVendor — Third-party vendors managed for risk assessment
model TPRMVendor {
  id                  String          @id @default(cuid())
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  vendorCode          String          // e.g., VEN001
  name                String
  contactEmail        String?
  contactPhone        String?
  accountManagerName  String?
  accountManagerEmail String?
  serviceCategory     String?         // e.g., "IT Services", "Cloud Infrastructure"
  departmentId        String?
  department          Department?     @relation("TPRMVendorDepartment", fields: [departmentId], references: [id])
  status              String          @default("Onboarding") // Onboarding, Onboarded, Offboarding, Offboarded
  engagementId        String?         // External engagement identifier
  vrr                 String?         // Vendor Risk Rating: Critical, High, Moderate, Low, Nominal
  serviceDescription  String?         @db.Text
  contractStartDate   DateTime?
  contractEndDate     DateTime?
  accessToNetwork     Boolean         @default(false)
  cloud               Boolean         @default(false)
  accessToData        Boolean         @default(false)
  pii                 Boolean         @default(false)
  businessJustification String?       @db.Text
  vendorCertification String?
  onboardedDate       DateTime?
  offboardedDate      DateTime?
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  assessments         TPRMAssessment[]
  monitoringVendor    TPRMMonitoringVendor? @relation("MonitoringToVendor")
  vendorIssues        TPRMVendorIssue[]

  @@unique([customerAccountId, vendorCode])
  @@index([customerAccountId])
  @@index([customerAccountId, status])
}

// TPRMAssessment — Vendor risk assessments
model TPRMAssessment {
  id                    String          @id @default(cuid())
  customerAccountId     String
  customerAccount       CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessmentCode        String          // e.g., ASM001
  vendorId              String
  vendor                TPRMVendor      @relation(fields: [vendorId], references: [id], onDelete: Cascade)
  assessmentType        String          // "Assessment Factory", "Onboarding Assessment", "Periodic Assessment", "On-Demand Assessment"
  status                String          @default("Draft") // Draft, In Progress, Submitted, Under Review, Reviewed, Approved, Rejected, Returned, Completed, Cancelled, Expired, Reassessment Initiated
  assessmentResult      String?         // Satisfactory, Unsatisfactory, Deficient
  vendorSubmissionDate  DateTime?
  assessorCompletionDate DateTime?
  approvalDate          DateTime?
  completionDate        DateTime?
  initiatedById         String?
  initiatedBy           User?           @relation("TPRMInitiator", fields: [initiatedById], references: [id])
  assessorId            String?
  assessor              User?           @relation("TPRMAssessor", fields: [assessorId], references: [id])
  approverId            String?
  approver              User?           @relation("TPRMApprover", fields: [approverId], references: [id])
  questionnaireTemplate String?         // Template name used for assessment
  approverComment       String?
  createdAt             DateTime        @default(now())
  updatedAt             DateTime        @updatedAt
  logs                  TPRMAssessmentLog[]
  responses             TPRMAssessmentResponse[]
  clarifications        TPRMClarification[]
  issueRemediations     TPRMIssueRemediation[]

  @@unique([customerAccountId, assessmentCode])
  @@index([customerAccountId])
  @@index([customerAccountId, assessmentType])
  @@index([customerAccountId, status])
}

// TPRMAssessmentLog — Audit logs for assessment activities
model TPRMAssessmentLog {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessmentId      String
  assessment        TPRMAssessment  @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  domainName        String?
  questionNo        String?
  questionTitle     String?
  logDate           DateTime        @default(now())
  logMessage        String?
  apiUrl            String?
  documentName      String?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
  @@index([assessmentId])
}

// TPRMConfiguration — Single flat record per customer with all DD + Scorecard values
model TPRMConfiguration {
  id                String          @id @default(cuid())
  customerAccountId String          @unique
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)

  // DueDiligence — VRR thresholds per risk category
  vrrCritical       Int             @default(50)
  vrrHigh           Int             @default(40)
  vrrModerate       Int             @default(30)
  vrrLow            Int             @default(20)
  vrrNominal        Int             @default(0)

  // DueDiligence — Assessment cadence (months)
  cadenceCritical   Int             @default(1)
  cadenceHigh       Int             @default(3)
  cadenceModerate   Int             @default(6)
  cadenceLow        Int             @default(24)
  cadenceNominal    Int             @default(36)

  // DueDiligence — Remediation period (days)
  remediationCritical Int           @default(7)
  remediationHigh     Int           @default(14)
  remediationModerate Int           @default(30)
  remediationLow      Int           @default(60)
  remediationNominal  Int           @default(90)

  // DueDiligence — Reminder before due date (days)
  reminderCritical  Int             @default(5)
  reminderHigh      Int             @default(5)
  reminderModerate  Int             @default(5)
  reminderLow       Int             @default(5)
  reminderNominal   Int             @default(5)

  // DueDiligence — Due date period (days)
  dueDateCritical   Int             @default(30)
  dueDateHigh       Int             @default(30)
  dueDateModerate   Int             @default(30)
  dueDateLow        Int             @default(30)
  dueDateNominal    Int             @default(30)

  // Scorecard — Security score per rating
  scorecardExcellent Int            @default(5)
  scorecardGood      Int            @default(4)
  scorecardModerate  Int            @default(3)
  scorecardLow       Int            @default(2)
  scorecardNominal   Int            @default(0)

  // Scorecard Configuration — formula & weights
  scoringFormula          String    @default("AVG")  // "AVG" or "SUM"
  securityPostureWeight   Int       @default(50)     // percentage
  threatExposureWeight    Int       @default(50)     // percentage

  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
}

// TPRMVendorProfileField — Vendor onboarding profile fields configuration
model TPRMVendorProfileField {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  fieldName         String
  isSystem          Boolean         @default(false) // System fields cannot be edited/deleted
  isActive          Boolean         @default(true)
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, fieldName])
  @@index([customerAccountId])
}

// TPRMOnboardingQuestion — Vendor onboarding questionnaire
model TPRMOnboardingQuestion {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  title             String
  question          String?         @db.Text
  score             Int             @default(0)
  questionType      String          @default("Parent") // "Parent" or "Child"
  parentId          String?
  parent            TPRMOnboardingQuestion?  @relation("OnboardingQuestionHierarchy", fields: [parentId], references: [id])
  children          TPRMOnboardingQuestion[] @relation("OnboardingQuestionHierarchy")
  responseType      String          @default("Yes/No") // "Yes/No" or "FreeText"
  isActive          Boolean         @default(true)
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
  @@index([parentId])
}

// TPRMServiceCategory — Service categories for vendor classification
model TPRMServiceCategory {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// TPRMDiscipline — Disciplines for vendor assessment (e.g., Compliance, Cyber)
model TPRMDiscipline {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// TPRMDepartment — TPRM-specific departments (separate from GRC departments)
model TPRMDepartment {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// TPRMQuestionnaireTemplate — Assessment questionnaire templates
model TPRMQuestionnaireTemplate {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  templateName      String
  frameworkName     String?
  templateCategory  String          @default("Default") // "Default", "ISMS", "Compliance"
  imageUrl          String?
  vendorProfileQuestionIds String?  @db.Text  // JSON array of onboarding question IDs
  isActive          Boolean         @default(true)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  masterQuestionLinks TPRMQuestionnaireQuestion[]

  @@unique([customerAccountId, templateName])
  @@index([customerAccountId])
}

// TPRMOffboardingQuestion — Vendor offboarding questionnaire
model TPRMOffboardingQuestion {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  sequenceNo        Int
  title             String
  question          String?         @db.Text
  isActive          Boolean         @default(true)
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
}

// TPRMScorecardFactor — Security scorecard factors (SecurityPosture & ThreatExposure)
model TPRMScorecardFactor {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  factorId          String          // e.g., "network_security", "known_breach"
  name              String          // Display name
  weightage         Int             @default(0) // percentage
  isMandatory       Boolean         @default(true)
  scoreType         String          // "SecurityPosture" or "ThreatExposure"
  isSystem          Boolean         @default(false) // System factors cannot be deleted
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, factorId])
  @@index([customerAccountId])
  @@index([customerAccountId, scoreType])
}

// TPRMDomain — Assessment domain categories (e.g., Information Security, Privacy)
model TPRMDomain {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  name              String
  description       String?
  isActive          Boolean         @default(true)
  sortOrder         Int             @default(0)
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt
  questions         TPRMMasterQuestion[]

  @@unique([customerAccountId, name])
  @@index([customerAccountId])
}

// TPRMMasterQuestion — Master question bank entries linked to domains
model TPRMMasterQuestion {
  id                  String          @id @default(cuid())
  customerAccountId   String
  customerAccount     CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  domainId            String?
  domain              TPRMDomain?     @relation(fields: [domainId], references: [id], onDelete: SetNull)
  questionText        String          @db.Text
  verifaiPrompt       String?         @db.Text
  sortOrder           Int             @default(0)
  isActive            Boolean         @default(true)
  // Parent-child hierarchy
  isParentQuestion    Boolean         @default(true)
  parentId            String?
  parent              TPRMMasterQuestion?  @relation("QuestionParent", fields: [parentId], references: [id], onDelete: SetNull)
  children            TPRMMasterQuestion[] @relation("QuestionParent")
  // Assessment metadata
  mandatoryAttachment Boolean         @default(false)
  validateThroughAI   Boolean         @default(false)
  mandatoryQuestion   Boolean         @default(false)
  evidence            String?
  issue               String?
  risk                String?
  recommendation      String?
  severity            String?         // High, Medium, Low
  createdAt           DateTime        @default(now())
  updatedAt           DateTime        @updatedAt
  questionnaireLinks  TPRMQuestionnaireQuestion[]

  @@index([customerAccountId])
  @@index([customerAccountId, domainId])
}

// TPRMQuestionnaireQuestion — Junction linking master questions to questionnaire templates
model TPRMQuestionnaireQuestion {
  id                String                    @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount           @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  templateId        String
  template          TPRMQuestionnaireTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)
  questionId        String
  question          TPRMMasterQuestion        @relation(fields: [questionId], references: [id], onDelete: Cascade)
  sortOrder         Int                       @default(0)
  createdAt         DateTime                  @default(now())

  @@unique([templateId, questionId])
  @@index([customerAccountId])
}

// ==================== TPRM MONITORING ====================

model TPRMMonitoringVendor {
  id                String    @id @default(cuid())
  customerAccountId String
  vendorName        String
  vendorURL         String
  vendorOnboarded   Boolean   @default(false)
  tprmVendorId      String?   @unique
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  customerAccount   CustomerAccount            @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessments       TPRMMonitoringAssessment[]
  tprmVendor        TPRMVendor?                @relation("MonitoringToVendor", fields: [tprmVendorId], references: [id], onDelete: SetNull)

  @@index([customerAccountId])
}

model TPRMMonitoringAssessment {
  id                        String    @id @default(cuid())
  customerAccountId         String
  monitoringVendorId        String
  vendorName                String
  vendorURL                 String
  jobID                     String?
  status                    String?
  overallSummary            String?   @db.Text
  overallScore              Int?
  securityPostureScore      Int?
  threatExposureScore       Int?
  securityPostureSummary    String?   @db.Text
  threatExposureSummary     String?   @db.Text
  lastScan                  DateTime?
  nextScan                  String?
  isLatest                  Boolean   @default(true)
  downloadType              String?
  calculatedSecurityPosture Float?
  calculatedThreatExposure  Float?
  calculatedOverallScore    Float?
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt

  customerAccount   CustomerAccount             @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  monitoringVendor  TPRMMonitoringVendor        @relation(fields: [monitoringVendorId], references: [id], onDelete: Cascade)
  complianceAndLegal TPRMComplianceAndLegal?
  recommendation    TPRMMonitoringRecommendation?
  kpiDetails        TPRMKPIDetail[]
  httpHeaders       TPRMHTTPHeader[]

  @@index([customerAccountId])
  @@index([monitoringVendorId])
}

model TPRMComplianceAndLegal {
  id               String    @id @default(cuid())
  assessmentId     String    @unique
  privacyPolicyUrl String?
  dpaUrl           String?
  createdAt        DateTime  @default(now())

  assessment       TPRMMonitoringAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  laws             TPRMLaw[]
  certifications   TPRMCertification[]
}

model TPRMLaw {
  id           String                 @id @default(cuid())
  complianceId String
  lawName      String
  createdAt    DateTime               @default(now())

  compliance   TPRMComplianceAndLegal @relation(fields: [complianceId], references: [id], onDelete: Cascade)
}

model TPRMCertification {
  id           String                 @id @default(cuid())
  complianceId String
  name         String
  createdAt    DateTime               @default(now())

  compliance   TPRMComplianceAndLegal @relation(fields: [complianceId], references: [id], onDelete: Cascade)
}

model TPRMMonitoringRecommendation {
  id           String    @id @default(cuid())
  assessmentId String    @unique
  statement    String    @db.Text
  createdAt    DateTime  @default(now())

  assessment   TPRMMonitoringAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
}

model TPRMKPIDetail {
  id                String    @id @default(cuid())
  assessmentId      String
  kpiName           String
  kpiType           String?
  securityScore     Int?
  summary           String?   @db.Text
  riskScore         Int?
  recommendation    String?   @db.Text
  cveId             String?
  severity          String?
  description       String?   @db.Text
  affectedComponent String?
  createdAt         DateTime  @default(now())

  assessment        TPRMMonitoringAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  keyFindings       TPRMKeyFinding[]
  sources           TPRMSource[]
  vulnerabilities   TPRMVulnerabilityFinding[]

  @@index([assessmentId])
}

model TPRMKeyFinding {
  id          String        @id @default(cuid())
  kpiDetailId String
  statement   String        @db.Text
  createdAt   DateTime      @default(now())

  kpiDetail   TPRMKPIDetail @relation(fields: [kpiDetailId], references: [id], onDelete: Cascade)
}

model TPRMSource {
  id          String        @id @default(cuid())
  kpiDetailId String
  name        String
  createdAt   DateTime      @default(now())

  kpiDetail   TPRMKPIDetail @relation(fields: [kpiDetailId], references: [id], onDelete: Cascade)
}

model TPRMVulnerabilityFinding {
  id                String        @id @default(cuid())
  kpiDetailId       String
  cveId             String?
  severity          String?
  affectedComponent String?
  description       String?       @db.Text
  createdAt         DateTime      @default(now())

  kpiDetail         TPRMKPIDetail @relation(fields: [kpiDetailId], references: [id], onDelete: Cascade)

  @@index([kpiDetailId])
}

model TPRMHTTPHeader {
  id             String    @id @default(cuid())
  assessmentId   String
  name           String
  present        Boolean   @default(false)
  value          String?
  recommendation String?   @db.Text
  description    String?   @db.Text
  createdAt      DateTime  @default(now())

  assessment     TPRMMonitoringAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  platforms      TPRMPlatform[]

  @@index([assessmentId])
}

model TPRMPlatform {
  id           String          @id @default(cuid())
  httpHeaderId String
  server       String
  createdAt    DateTime        @default(now())

  httpHeader   TPRMHTTPHeader  @relation(fields: [httpHeaderId], references: [id], onDelete: Cascade)
}

model TPRMMonitoringSchedule {
  id                String   @id @default(uuid())
  customerAccountId String   @unique
  recurrence        String   @default("none") // none, daily, weekly, monthly, custom
  customDays        Int?                       // only used when recurrence = "custom"
  lastScheduledRun  DateTime?
  nextScheduledRun  DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
}

// TPRMAssessmentResponse — Per-question answers from Account Manager
model TPRMAssessmentResponse {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessmentId      String
  assessment        TPRMAssessment  @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  questionId        String
  domainId          String?
  questionNo        String?
  response          String?         // "Yes", "No", "NA"
  comment           String?         @db.Text
  artifactUrl       String?
  artifactName      String?
  isFlagged         Boolean         @default(false)
  isDelegated       Boolean         @default(false)
  delegatedToId     String?
  delegatedTo       User?           @relation("TPRMResponseDelegatedTo", fields: [delegatedToId], references: [id])
  respondedById     String?
  respondedBy       User?           @relation("TPRMResponseRespondedBy", fields: [respondedById], references: [id])
  respondedAt       DateTime?
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([assessmentId, questionId])
  @@index([customerAccountId])
  @@index([assessmentId])
}

// TPRMClarification — Assessor requests clarification on a question
model TPRMClarification {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessmentId      String
  assessment        TPRMAssessment  @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  questionNo        String?
  domainName        String?
  rejectComment     String?         @db.Text
  amResponse        String?         @db.Text
  status            String          @default("Pending") // Pending, Submitted, Closed
  requestedById     String?
  requestedBy       User?           @relation("TPRMClarificationRequestedBy", fields: [requestedById], references: [id])
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
  @@index([assessmentId])
}

// TPRMIssueRemediation — Issue remediation requests for Account Manager
model TPRMIssueRemediation {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  assessmentId      String
  assessment        TPRMAssessment  @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  domainName        String?
  severity          String?         // Critical, High, Medium, Low
  description       String?         @db.Text
  amResponse        String?         @db.Text
  requestedDate     DateTime?
  dueDate           DateTime?
  status            String          @default("Pending") // Pending, Submitted, Closed
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
  @@index([assessmentId])
}

// TPRMVendorIssue — Vendor-reported issues by Account Manager
model TPRMVendorIssue {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  vendorId          String
  vendor            TPRMVendor      @relation(fields: [vendorId], references: [id], onDelete: Cascade)
  title             String
  description       String?         @db.Text
  severity          String?         // Critical, High, Medium, Low
  dueDate           DateTime?
  resolution        String?         @db.Text
  status            String          @default("Open") // Open, Submitted, Closed
  reportedById      String?
  reportedBy        User?           @relation("TPRMVendorIssueReportedBy", fields: [reportedById], references: [id])
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@index([customerAccountId])
  @@index([vendorId])
}

// ==================== REGULATORY INTELLIGENCE ====================

model RegulatoryProfile {
  id                    String          @id @default(cuid())
  customerAccountId     String
  customerAccount       CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)

  // Legal Identity
  fullLegalEntityName   String
  registrationNo        String
  url                   String
  country               String

  // Scope and Presence
  industrySectors       String          @db.Text // JSON array
  otherIndustry         String?
  organisationType      String
  countriesOfOperation  String          @db.Text // JSON array

  // Physical Presence
  headquarterAddress    String
  adminContactEmail     String

  // Regional Preference
  timeZone              String
  language              String

  // Business Context for AI
  businessModel         String
  targetAudience        String          @db.Text // JSON array
  technologyUsed        String          @db.Text // JSON array

  createdAt             DateTime        @default(now())
  updatedAt             DateTime        @updatedAt
  createdBy             String?

  // Relation to suggested regulations
  suggestedRegulations  SuggestedRegulation[]

  @@index([customerAccountId])
}

// Suggested Regulations from AI (Regulatory Intelligence Hub)
model SuggestedRegulation {
  id                    String            @id @default(cuid())
  customerAccountId     String
  customerAccount       CustomerAccount   @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  regulatoryProfileId   String
  regulatoryProfile     RegulatoryProfile @relation(fields: [regulatoryProfileId], references: [id], onDelete: Cascade)

  // Regulation details from AI
  name                  String            // Regulation/Framework name
  type                  String            // "Regulatory Requirement", "Industry Standard", "Maturity Enhancing Framework"
  applicability         String            // "Mandatory", "Recommended", "Optional"
  reason                String            @db.Text // Justification from AI

  // Framework linking (for subscription)
  masterFrameworkId     String?           // Link to master framework if available
  isSubscribed          Boolean           @default(false)

  createdAt             DateTime          @default(now())
  updatedAt             DateTime          @updatedAt

  @@index([customerAccountId])
  @@index([regulatoryProfileId])
  @@index([customerAccountId, regulatoryProfileId])
}

// ==================== DYNAMIC DATA TRANSLATIONS ====================

// DynamicTranslation stores auto-translated versions of user-entered data
// (risk names, policy descriptions, audit findings, etc.) for non-English locales.
// A single generic table — no schema changes needed per model.
model DynamicTranslation {
  id                String          @id @default(cuid())
  customerAccountId String
  customerAccount   CustomerAccount @relation(fields: [customerAccountId], references: [id], onDelete: Cascade)
  modelName         String          // "Risk", "Control", "Framework", etc.
  recordId          String          // ID of the source record
  fieldName         String          // "name", "description", etc.
  locale            String          // "ar", "lv" (never "en")
  translatedText    String          @db.Text
  sourceHash        String?         // Hash of original text for staleness detection
  isStale           Boolean         @default(false)
  translatedBy      String          @default("azure") // "azure", "manual"
  createdAt         DateTime        @default(now())
  updatedAt         DateTime        @updatedAt

  @@unique([customerAccountId, modelName, recordId, fieldName, locale])
  @@index([customerAccountId, modelName, recordId])
  @@index([customerAccountId, modelName, locale])
  @@index([isStale])
}

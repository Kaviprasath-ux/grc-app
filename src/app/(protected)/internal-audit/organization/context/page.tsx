/**
 * IA Organization > Context — re-exports the existing /organization/context page.
 * Same content for both modules; only the URL differs.
 */
export { default } from "@/app/(protected)/organization/context/page";
